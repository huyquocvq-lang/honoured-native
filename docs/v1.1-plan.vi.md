# Kế hoạch triển khai V1.1 — phần native iOS

> Phiên bản: 1.0 · Soạn ngày 10/09/2026 · Phạm vi: `honoured-native` (iOS)
> Bản tiếng Việt. Bản tiếng Anh gửi khách sẽ được rút gọn từ tài liệu này.

---

## 1. Tóm tắt phạm vi

### 1.1 Kiến trúc thực tế

App iOS hiện tại là **native shell (`WKWebView`) bọc web app Lovable** tại `honour-your-word.lovable.app`. Lớp native hiện chỉ gồm 7 file Swift và đảm nhiệm billing (RevenueCat) cùng một bridge message tối giản.

| Thành phần | Hiện trạng |
|---|---|
| `ios/Honoured/HonouredWebView.swift` | WKWebView + navigation policy + microphone permission |
| `ios/Honoured/NativeBridge.swift` | Bridge v1: `APP_READY`, `CHECK_ACCESS`, `IDENTIFY_USER`, `LOGOUT_USER`, `START_PURCHASE`, `RESTORE_PURCHASES` |
| `ios/Honoured/SubscriptionService.swift` | RevenueCat, entitlement `honoured_plus` |
| `ios/project.yml` | XcodeGen, **một target duy nhất**, iOS 16.0, Swift 5.9 |
| `ios/Honoured/Info.plist` | Chưa có key HealthKit, chưa có background modes |
| Entitlements | **Chưa tồn tại file nào** |

Toàn bộ UI nghiệp vụ (Contract, Progress, Journal, Calendar, Settings, Testament Timer UI) nằm ở web app — **không thuộc phạm vi tài liệu này**, xem mục 7 cho phần phụ thuộc.

### 1.2 Quyết định đã chốt với khách

| Vấn đề | Chốt |
|---|---|
| Dynamic Island | Dời sang M3 (tháng 10), không chặn bản 28/9 |
| Thông báo | **Local notification**, không dùng server push / APNs |
| Giấc ngủ | Ẩn chỉ số khi không có dữ liệu |
| Đăng nhập | V1.1 chỉ Sign in with Apple; Google dời sang M3 |
| Đồng bộ nền | Best-effort, không cam kết mốc giờ cứng |
| Android | Ngoài phạm vi (V1.2+) |

---

## 2. Mốc thanh toán & phạm vi bàn giao

| Mốc | Ngày | Tiền | Bàn giao | Giờ |
|---|---|---|---|---|
| **M1** | 08/09 | $500 | Chốt scope, bridge contract v2, schema Supabase, project config | — |
| **M2** | 28/09 | $700 | V1.1 lên App Store (trừ Dynamic Island + Google) | ~135h |
| **M3** | 12/10 | $500 | Dynamic Island + Google Sign-In | ~40h |
| | | **$1,700** | | **~175h** |

> **Lưu ý ngày soạn:** tài liệu viết ngày 10/09, cửa sổ M2 đã trôi 2 ngày. Repo chưa có commit nào sau 07/09 → toàn bộ task dưới đây tính từ mốc 0. Cần đối chiếu lại tiến độ Tuần 1 trước khi bắt tay.

---

## 3. Nền tảng kỹ thuật — phải chốt trước khi viết code

Ba phần dưới đây là "hợp đồng" giữa native / web / Supabase. Sai ở đây thì mọi task sau phải sửa lại, nên đây là việc đầu tiên của Tuần 1.

### 3.1 Bridge v2 — hợp đồng message

**Nguồn chính thức: [`docs/bridge.md`](bridge.md)** — đã chốt ngày 13/09 (M1-01 xong). Không lặp lại bảng ở đây để tránh hai bản lệch nhau.

Tóm tắt các nhóm message v2 (toàn bộ v1 giữ nguyên):

| Nhóm | Web → Native | Native → Web |
|---|---|---|
| Auth session | `SET_AUTH_SESSION`, `CLEAR_AUTH_SESSION` | `AUTH_SESSION_UPDATED`, `AUTH_SESSION_INVALID` |
| HealthKit | `REQUEST_HEALTH_PERMISSION`, `GET_HEALTH_STATUS`, `QUERY_HEALTH_METRICS`, `SET_GOALS`, `SET_DAY_RESET_HOUR` | `HEALTH_PERMISSION_STATUS`, `HEALTH_METRICS`, `GOAL_REACHED`, `HEALTH_DATA_UPDATED` |
| Timer | `START_TIMER`, `CANCEL_TIMER`, `GET_TIMER_STATE` | `TIMER_STARTED`, `TIMER_COMPLETED`, `TIMER_CANCELLED`, `TIMER_STATE` |
| Nghi thức | `ACTIVITY_COMPLETED`, `SET_SOUND_ENABLED` | `NOTIFICATION_OPENED` |
| Apple | `SIGN_IN_WITH_APPLE` | `APPLE_SIGN_IN_SUCCESS`, `APPLE_SIGN_IN_FAILED` |

Những điểm khác so với bản nháp ban đầu của plan này, đã chốt trong `bridge.md`:

- **Thêm nhóm auth session** — native ghi Supabase dưới RLS nên cần user token; web đẩy xuống, native tự refresh chỉ khi chạy nền.
- **Quãng đường tách 3 metric** (`distance_walking_running`, `distance_cycling`, `distance_swimming`) — goal đạp xe không được tính bằng đi bộ. Tổng 9 identifier.
- **Đọc thêm `basal_energy`** để không phải sửa native dù Open Item #1 trả lời thế nào.
- **`GET_TIMER_STATE` + `TIMER_STARTED { endsAt }`** — web reload giữa chừng vẫn bắt lại timer; web lấy giờ từ `endsAt`, không tự `setInterval`.
- **`GOAL_REACHED` broadcast** — web giữ state contract nên native phải báo lên.
- **Web phải dùng `linkIdentity`** khi link Apple, không phải `signInWithIdToken`, nếu không mất `user_id` ẩn danh.
- Hàng đợi broadcast khi WebView chưa `APP_READY` (giới hạn 50, sống qua reload) — đã implement trong `NativeBridge.swift`.

### 3.2 Schema Supabase

```sql
-- Mẫu thô từ HealthKit, khoá chống trùng là UUID của sample
create table health_samples (
  id           uuid primary key default gen_random_uuid(),
  user_id      uuid not null references auth.users(id) on delete cascade,
  sample_uuid  uuid not null,              -- HKSample.uuid
  metric       text not null,              -- steps | distance | active_energy | heart_rate | sleep | exercise_minutes
  value        double precision not null,
  unit         text not null,
  started_at   timestamptz not null,
  ended_at     timestamptz not null,
  source_name  text,                       -- tên app/thiết bị ghi dữ liệu
  deleted_at   timestamptz,                -- soft delete khi HealthKit báo sample bị xoá
  created_at   timestamptz default now(),
  unique (user_id, sample_uuid, metric)
);
create index on health_samples (user_id, metric, started_at desc);

-- Bảng tổng hợp theo ngày để Progress tab đọc trực tiếp, không phải quét sample thô
create table health_daily (
  user_id    uuid not null,
  day        date not null,
  metric     text not null,
  total      double precision not null,
  unit       text not null,
  updated_at timestamptz default now(),
  primary key (user_id, day, metric)
);
```

- RLS: cả hai bảng chỉ cho phép `auth.uid() = user_id`.
- Native gọi RPC `upsert_health_samples(batch jsonb)` — upsert theo `on conflict (user_id, sample_uuid, metric)`, đánh dấu `deleted_at` cho UUID bị xoá.
- **`health_daily.total` KHÔNG derive từ sample thô ở server** (sửa ngày 13/09). iPhone và Apple Watch cùng ghi bước chân → cộng sample thô sẽ đếm đôi. Native tính tổng ngày bằng `HKStatisticsQuery` (tự khử trùng theo source, đúng như Health app hiển thị) rồi upsert thẳng vào `health_daily` qua RPC `upsert_health_daily(rows jsonb)`. Sample thô vẫn upload để audit và để xử lý xoá.
- **Anchor không lưu trên server.** `HKQueryAnchor` gắn với từng thiết bị → archive bằng `NSKeyedArchiver` và lưu `UserDefaults` theo key mỗi metric. Chỉ commit anchor **sau khi** batch đã upload hoặc đã ghi vào hàng đợi offline — commit sớm là mất sample nếu upload fail.
- Lần sync đầu chỉ đọc lùi 30 ngày (`initialHistoryDays`), mốc này cố định trong `UserDefaults` để mọi anchored query sau dùng cùng predicate.

### 3.3 Chiến lược chống trùng + offline

| Vấn đề | Cách xử lý |
|---|---|
| Trùng dữ liệu | `HKAnchoredObjectQuery` chỉ trả sample mới/đổi từ anchor lần trước; server thêm một lớp bảo hiểm bằng `unique (user_id, sample_uuid, metric)` |
| Sample bị xoá trong Health | Anchored query trả `HKDeletedObject` → set `deleted_at`, trừ lại vào `health_daily` |
| Mất mạng | Batch chưa gửi được ghi xuống file JSON trong Application Support; `NWPathMonitor` bắt sự kiện có mạng trở lại → gửi lại theo thứ tự, backoff luỹ thừa |
| Nhiều thiết bị | Sample UUID là duy nhất toàn cục nên hai thiết bị đọc cùng dữ liệu Health sẽ upsert vào cùng dòng, không nhân đôi |
| iPhone + Watch cùng ghi | Không cộng sample thô. Tổng ngày lấy từ `HKStatisticsQuery` trên máy (đã khử trùng theo source) — xem 3.2 |
| Qua nửa đêm / đổi múi giờ | `health_daily.day` tính theo **giờ reset hằng ngày mà user đặt trong Settings**, không phải UTC — cần web đẩy giá trị này xuống native |

---

## 4. MILESTONE 1 — 08/09 — Chốt scope ($500)

Không có code sản phẩm; đây là mốc khoá phạm vi và dựng nền.

| ID | Task | Ước tính |
|---|---|---|
| M1-01 | Chốt bridge v2 contract, cập nhật `docs/bridge.md` | 3h |
| M1-02 | Chốt schema Supabase + RLS, review với người làm web | 3h |
| M1-03 | Chốt danh sách 7 metric và ánh xạ sang HealthKit identifier | 1h |
| M1-04 | Xác nhận lại 4 quyết định của khách bằng văn bản | 1h |

**Điều kiện hoàn thành:** `docs/bridge.md` đã có phần v2; schema đã chạy được trên Supabase staging; người làm web xác nhận đọc/hiểu hợp đồng message.

---

## 5. MILESTONE 2 — 08–28/09 — V1.1 lên store ($700, ~135h)

### Tuần 1 (08–14/09) — Nền tảng HealthKit · ~45h

| ID | Task | Chi tiết | Giờ |
|---|---|---|---|
| N-01 | Bridge v2 khung sườn | Nâng `bridgeVersion` = 2, thêm nhánh `switch` cho message mới, cơ chế xếp hàng event khi WebView chưa sẵn sàng | 4h |
| N-02 | Cấu hình project | Tạo `Honoured.entitlements` (HealthKit), thêm `NSHealthShareUsageDescription` vào Info.plist, khai báo entitlements trong `project.yml`, bật capability HealthKit trên Apple Developer portal | 5h |
| N-03 | `HealthKitService` — quyền | `requestAuthorization` cho 7 loại đọc, đọc `authorizationStatus` từng loại, xử lý case user chỉ cho một phần | 6h |
| N-04 | Đọc dữ liệu | `HKAnchoredObjectQuery` cho từng metric, lưu/khôi phục anchor, gom sample thành batch | 10h |
| N-05 | Supabase: bảng + RPC | Tạo 2 bảng, RLS, RPC `upsert_health_samples` kèm rollup `health_daily` | 8h |
| N-06 | Sync client | Gửi batch lên Supabase, xử lý `HKDeletedObject`, map lỗi mạng/HTTP | 6h |
| N-07 | Hàng đợi offline | Ghi batch chưa gửi xuống đĩa, `NWPathMonitor`, retry backoff | 4h |
| N-08 | Bridge messages HealthKit | `REQUEST_HEALTH_PERMISSION`, `GET_HEALTH_STATUS`, `QUERY_HEALTH_METRICS`, `SET_GOALS` | 2h |

**Ánh xạ metric ↔ HealthKit**

| Metric | HealthKit identifier |
|---|---|
| Bước chân | `HKQuantityTypeIdentifier.stepCount` |
| Quãng đường | `.distanceWalkingRunning`, `.distanceCycling`, `.distanceSwimming` |
| Năng lượng vận động | `.activeEnergyBurned` |
| Nhịp tim | `.heartRate` |
| Số phút vận động | `.appleExerciseTime` |
| Giấc ngủ | `HKCategoryTypeIdentifier.sleepAnalysis` |

**Điều kiện hoàn thành Tuần 1:** mở app trên máy thật → hiện sheet quyền → dữ liệu bước chân/quãng đường/calo của hôm nay xuất hiện trong bảng `health_daily` trên Supabase. Bật máy bay, đi bộ, bật mạng lại → dữ liệu tự lên, không nhân đôi dòng.

### Tuần 2 (15–21/09) — Nghi thức & danh tính · ~50h

| ID | Task | Chi tiết | Giờ |
|---|---|---|---|
| N-09 | Đồng bộ nền | `HKObserverQuery` + `enableBackgroundDelivery(frequency: .hourly)` cho từng metric, xử lý completion handler đúng cách (bắt buộc, nếu không iOS sẽ ngừng đánh thức app) | 7h |
| N-10 | `BGTaskScheduler` | Đăng ký `BGAppRefreshTask` làm đường dự phòng khi observer không kích hoạt, thêm `UIBackgroundModes` + `BGTaskSchedulerPermittedIdentifiers` | 4h |
| N-11 | Hạ tầng notification | Xin quyền `UNUserNotificationCenter` **đúng thời điểm** (lần đầu user bật timer, không phải lúc mở app), `UNUserNotificationCenterDelegate` | 4h |
| N-12 | Testament Timer native | `START_TIMER` → `UNTimeIntervalNotificationTrigger` với id `timer-<activityId>`; `CANCEL_TIMER` → huỷ; app quay lại foreground trước hạn thì huỷ notification và bắn `TIMER_COMPLETED` | 8h |
| N-13 | Phát hiện đạt mục tiêu ở nền | So tổng hôm nay với goal đã nhận qua `SET_GOALS`; chỉ bắn **một lần/hoạt động/ngày** (lưu cờ đã báo); bỏ qua nếu app đang `.active` để web tự lo phần ăn mừng trong app | 8h |
| N-14 | Deep link | `userInfo.activityId` → gửi `NOTIFICATION_OPENED`; xếp hàng nếu WebView chưa `APP_READY` | 3h |
| N-15 | Tiếng cồng | `AVAudioSession` category `.ambient` (tôn trọng công tắc im lặng — **không dùng `.playback`**), đóng gói file âm thanh cho notification, `SET_SOUND_ENABLED` quyết định notification có kèm tiếng hay không | 6h |
| N-16 | Sign in with Apple | Bật capability, sinh nonce (SHA256), `ASAuthorizationController`, trả `identityToken` + `rawNonce` cho web để gọi `supabase.auth.linkIdentity` trên anonymous user hiện tại; kiểm tra `getCredentialState` lúc khởi động | 10h |

#### Kế hoạch thực thi Tuần 2 — 15–21/09

**Điều kiện đầu vào và giả định an toàn**

- Publish Lovable commit `9c13bdf7e6cf5057717d0eaf671519c354790f85` trước khi chạy E2E; bundle production kiểm tra ngày 15/09 vẫn chưa có bridge v2.
- Apple Developer cần bật HealthKit, Background Delivery và Sign in with Apple cho App ID dùng để test. Thiếu quyền portal không chặn viết code/simulator, nhưng chặn toàn bộ nghiệm thu máy thật của N-09, N-10 và N-16.
- Khi không có contract/goal cho ngày hiện tại, web gửi danh sách không chứa activity đó (hoặc `goals: []`); native không tự dùng goal cũ và không gửi notification. Đây là mặc định bảo thủ cho tới khi khách xác nhận khác.
- N-15 cần một file gong đã được duyệt, định dạng `.caf`, `.aiff` hoặc `.wav`, dưới 30 giây. Nếu chưa có asset, hoàn thiện setting/audio-session trước và giữ notification không có gong vì mặc định sound là tắt.
- Luồng giữ nguyên user khi liên kết Apple cần web chạy anonymous-by-default và dùng `supabase.auth.linkIdentity`. Native có thể hoàn thành payload/token flow độc lập, nhưng N-16 chỉ nghiệm thu E2E sau khi phía web đáp ứng điều kiện này.

**Thứ tự triển khai**

| Ngày | Task | Phần làm cụ thể | Kết quả phải có |
|---|---|---|---|
| 15/09 | N-09 | Tách `collectAndEnqueue` khỏi `drainQueue`; tạo `HKObserverQuery` cho 9 metric, đăng ký `enableBackgroundDelivery(.hourly)`. Completion của observer được gọi sau khi dữ liệu đã ghi bền vững, không chờ upload/retry mạng. | Observer không bị giữ bởi backoff; nhiều wake đồng thời không tạo nhiều vòng sync. |
| 16/09 | N-10 | Đăng ký `BGAppRefreshTask` trước khi app launch xong, schedule lại sau mỗi lần chạy, xử lý expiration/cancel và gọi cùng pipeline collect/enqueue. Rà lại `UIBackgroundModes` và identifier trong `Info.plist`. | Có fallback độc lập với observer; handler luôn kết thúc và reschedule. |
| 17/09 | N-11 + N-14 nền | Tạo `NotificationCoordinator`, delegate và permission flow chỉ khi timer đầu tiên hoặc goal list đầu tiên có dữ liệu. Thêm kho event native bền vững, giới hạn kích thước, để giữ notification tap/goal/timer qua cold start. | Permission không hỏi lúc launch; event cold-start được flush đúng một lần sau khi web ready. |
| 18/09 | N-12 + N-14 | Tạo `TimerStore` một-timer, lưu `activityId`/`endsAt`, nối `START_TIMER`, `CANCEL_TIMER`, `GET_TIMER_STATE`, foreground completion và notification tap deep link. Chặn completion trùng giữa timer, delegate và app lifecycle. | Timer sống qua reload/relaunch; cancel đúng notification; tap mở đúng activity. |
| 19/09 | N-13 | Sau mỗi health collection, so tổng theo reset-hour với goal hiện tại; lưu khóa `(activityId, healthDay)` đã xử lý. Nối `ACTIVITY_COMPLETED`, `GOAL_REACHED`, active/background rule và notification một lần/ngày. | Một goal chỉ phát một event/notification mỗi health day; đổi user/reset-hour không dùng marker cũ. |
| 20/09 | N-15 + N-16 nền | Persist `SET_SOUND_ENABLED` mặc định false; cấu hình `.ambient`; gắn sound vào timer/goal notification khi bật. Tạo nonce ngẫu nhiên, SHA256 và coordinator `ASAuthorizationController`. | Silent switch được tôn trọng; Apple response không log token/nonce. |
| 21/09 | N-16 + tích hợp | Nối `SIGN_IN_WITH_APPLE`, map cancel/error, trả token/nonce/code/profile-first-login; kiểm tra credential state. Audit toàn bộ requestId, queue, account switch và contract Lovable. | Native Apple flow hoàn chỉnh; E2E link identity giữ nguyên `userId` khi web và Apple capability sẵn sàng. |

**Thứ tự phụ thuộc**

1. N-09 phải ổn định pipeline background trước N-10 và N-13.
2. N-11 cung cấp permission/delegate cho N-12, N-13, N-14 và N-15.
3. N-12 phải có completion idempotent trước khi thêm deep link và gong.
4. N-13 dùng `SET_GOALS`, reset-hour và daily totals của Tuần 1; không tính lại từ raw samples.
5. N-16 native có thể làm song song, nhưng nghiệm thu cuối phụ thuộc Apple portal và anonymous/linkIdentity phía Lovable.

**Ma trận kiểm tra cuối tuần**

| Nhóm | Kiểm tra tập trung | Giới hạn báo cáo |
|---|---|---|
| Tĩnh | `swiftc -parse`, `plutil -lint`, `git diff --check`; đối chiếu toàn bộ message với `docs/bridge.md` | Chỉ xác nhận cú pháp/cấu hình, không thay cho build. |
| Simulator | Timer start/cancel/restore, event queue, notification permission timing, requestId và duplicate-completion bằng bridge stub | Không xác nhận HealthKit background delivery, Apple Sign-In thật hoặc silent switch vật lý. |
| Máy thật | Khoá máy khi timer chạy; tap notification cold start; observer/BG refresh; airplane mode; goal một lần/ngày; gong bật/tắt + silent switch; Apple link identity | Chỉ đánh dấu N-09/N-10/N-16 Done sau khi capability/signing hoạt động trên thiết bị. |
| Lovable | Deploy đúng commit, nhận/flush event v2, điều hướng activity, cập nhật contract, `linkIdentity`, gửi lại `SET_AUTH_SESSION` | Typecheck/test/build pass không đồng nghĩa đã publish hoặc E2E production pass. |

**Điểm kiểm soát cuối mỗi ngày**

- Ghi task nào đã code, task nào đã chạy được trên simulator/máy thật và blocker còn lại; không gộp “code complete” với “device verified”.
- Không để observer/BG handler chờ retry mạng vô hạn; dữ liệu phải vào queue bền vững rồi trả completion.
- Không log access token, refresh token, Apple identity token, authorization code hoặc nonce.
- Sau đổi user phải xoá timer/goal marker/event nhạy theo account hoặc ràng buộc chúng bằng `userId` trước khi xử lý.

**Điều kiện hoàn thành Tuần 2:** bật timer 5 phút → khoá máy → hết giờ có notification, chạm vào mở đúng activity. Đi bộ vượt mục tiêu khi app ở nền → nhận notification một lần duy nhất trong ngày. Đăng nhập Apple từ tài khoản ẩn danh → dữ liệu HealthKit cũ vẫn thuộc về user đó.

### Tuần 3 (22–28/09) — Kiểm thử & phát hành · ~40h

| ID | Task | Chi tiết | Giờ |
|---|---|---|---|
| N-17 | QA ma trận quyền | Chưa hỏi / từ chối toàn bộ / cho một phần / thu hồi giữa chừng / thiết bị không có dữ liệu Health | 8h |
| N-18 | QA nền & biên | Kill app rồi vẫn phải sync; máy bay; đổi múi giờ; qua nửa đêm; đổi giờ reset trong Settings; pin yếu / Low Power Mode | 8h |
| N-19 | Sửa lỗi từ QA | Quỹ dự phòng | 10h |
| N-20 | Chuẩn bị App Store | Privacy nutrition label khai báo health data, privacy policy URL, mô tả rõ mục đích dùng HealthKit trong phần review notes | 5h |
| N-21 | TestFlight | Build, smoke test trên máy thật, mời khách test | 4h |
| N-22 | Nộp App Store | Nộp + xử lý phản hồi review | 5h |

**Cảnh báo:** app dùng HealthKit bị review kỹ hơn mức bình thường. Thiếu privacy policy hoặc mô tả mục đích mập mờ là lý do bị từ chối phổ biến nhất. N-20 không phải việc giấy tờ cho có — làm sớm trong tuần, đừng để đến ngày nộp.

---

## 6. MILESTONE 3 — 29/09–12/10 — Dynamic Island + Google ($500, ~40h)

**Cập nhật thiết kế 23/09:** dùng [plan nhiều Live Activities theo contract](live-activities-plan.vi.md) làm tài liệu triển khai cho phần Live Activity. Mỗi contract đang theo dõi có một thẻ; contract được mở/Start gần nhất có độ ưu tiên cao nhất, các thẻ khác vẫn được cập nhật. Bảng giờ dưới là baseline M3 ban đầu, không phải estimate cho phạm vi mở rộng; plan chi tiết tách gói việc, web integration và QA. Chưa triển khai feature này và chưa thay đổi thỏa thuận thương mại.

| ID | Task | Chi tiết | Giờ baseline |
|---|---|---|---|
| N-23 | Target Widget Extension | Thêm `HonouredWidgets`, shared DTO, `NSSupportsLiveActivities`; đề xuất feature từ iOS 16.2, app giữ 16.0; App Group chỉ khi thật sự cần shared storage | 6h |
| N-24 | Live Activity UI | Lock Screen + compact leading/trailing, minimal, expanded; timer/Health/mixed, unknown/stale/completed | 8h |
| N-25 | Vòng đời nhiều Live Activities | Một thẻ mỗi contract occurrence; focus bằng relevance score; restore/account/day cleanup; giữ một timer nghiệp vụ | 6h |
| N-26 | Celebration khi hoàn thành | Final state + transition theo OS; không cam kết nhấp nháy đúng 3 lần hoặc animation trên Always-On; xem giới hạn và dismissal policy trong plan chi tiết | 4h |
| N-27 | Tiến độ HealthKit | Cập nhật mọi contract đang theo dõi, không chỉ lúc đạt goal; cache khi khóa máy; completion theo policy tương thích web | 4h |
| N-28 | Google Sign-In — iOS + Android | Xem [plan chi tiết Google Sign-In](google-sign-in-plan.vi.md): iOS GoogleSignIn SDK, Android Credential Manager, web Supabase sign-in/link, bridge an toàn và QA hai nền tảng | 8h (baseline iOS cũ) |
| N-29 | QA + nộp bản cập nhật | | 4h |

**Cập nhật phạm vi Google 24/09:** yêu cầu mới bao gồm **cả iOS và Android**. [Plan Google Sign-In](google-sign-in-plan.vi.md) đề xuất **42–60h** cho native hai nền tảng + web + cấu hình + QA, chưa gồm thời gian chờ quyền/thiết bị/store. Đây là plan, chưa implement và chưa điều chỉnh thỏa thuận thương mại M3. Không tự chuyển web sang anonymous-by-default; tách đăng nhập và liên kết để bảo toàn tài khoản hiện có.

**Quyết định và giới hạn:** đã chọn hiển thị cả Dynamic Island và Lock Screen. HealthKit có thể không đọc được dữ liệu mới lúc máy khóa; countdown không bảo đảm app được đánh thức để end thẻ đúng deadline. Các giới hạn, fallback và nguồn Apple được ghi trong mục 3 của plan chi tiết; cần dùng chúng làm acceptance khi triển khai.

---

## 7. Phụ thuộc từ phía web app (Lovable)

Native làm xong không có nghĩa là V1.1 ship được. Những việc dưới đây thuộc bên web, cần theo dõi song song.

**Đã kiểm tra code Lovable (HEAD, 15/09, qua MCP — chỉ đọc):** `src/lib/native-bridge.ts` mới có nhóm billing v1, `APP_READY`, auth session, HealthKit permission/status/query, `SET_GOALS`, `SET_DAY_RESET_HOUR`, `HEALTH_DATA_UPDATED`, `AUTH_SESSION_*`. `routes/index.tsx` đã gọi `notifyAppReady()`, `sendDayResetHour()` và `sendGoals()` đúng lúc (mount + khi đổi). **Chưa có** bất kỳ message nào của timer, notification, sound, `ACTIVITY_COMPLETED`, `GOAL_REACHED`, Apple. Bản production đang deploy còn chưa có cả bridge v2.

### 7.1 Còn thiếu phía Lovable — theo thứ tự ưu tiên

| # | Việc | File gợi ý | Chi tiết / vì sao |
|---|---|---|---|
| L-1 | **Publish commit `9c13bdf`** (bridge v2 tuần 1) | — | Production bundle hiện chưa có `SET_AUTH_SESSION` → native không sync được gì trên máy thật. |
| L-2 | Thêm type mới vào `OutboundType` / `InboundType` | `native-bridge.ts` | Out: `START_TIMER`, `CANCEL_TIMER`, `GET_TIMER_STATE`, `ACTIVITY_COMPLETED`, `SET_SOUND_ENABLED`, `SIGN_IN_WITH_APPLE`. In: `TIMER_STARTED`, `TIMER_CANCELLED`, `TIMER_STATE`, `TIMER_COMPLETED`, `GOAL_REACHED`, `NOTIFICATION_OPENED`, `ACTIVITY_COMPLETION_ACCEPTED`, `SOUND_STATE`, `APPLE_SIGN_IN_SUCCESS/FAILED`. Hiện `onNativeBroadcast` bỏ qua mọi type không có trong danh sách. |
| L-3 | **Testament Timer chạy bằng native khi có bridge** | `timer-store.ts`, `Contract.tsx` | `testamentTimer.start()` → `START_TIMER { activityId: contract.id, activityName: primaryActivity, durationSeconds }`; lấy `endsAt` từ `TIMER_STARTED` thay vì `Date.now()+…`; nút X → `CANCEL_TIMER`. Sau mỗi `NATIVE_READY` gọi `GET_TIMER_STATE` để khôi phục sau reload. |
| L-4 | **Pause/Resume** — contract native không có | `timer-store.ts`, `Contract.tsx` | Web đang có nút Pause/Resume. Hai lựa chọn: (a) trong shell native ẩn Pause (khớp copy "Only a natural finish stamps this Honoured"), hoặc (b) map pause = `CANCEL_TIMER`, resume = `START_TIMER` với `remaining` giây (dùng được ngay, không cần message mới). **Cần khách chốt.** |
| L-5 | **`TIMER_COMPLETED` là nguồn hoàn thành duy nhất** | `timer-store.ts` `resolveFinishedTimer()` | Hiện web tự đếm mỗi giây và **bỏ** timer kết thúc trước lúc app mở (`endsAt < SESSION_START` → "never sat through"). Với native, timer hết giờ khi app ở nền/tắt là hợp lệ: nhận `TIMER_COMPLETED { activityId, completedAt, notified }` → `honourContract(activityId, Date.parse(completedAt))`; nếu `notified:false` (app đang mở) chạy Completion Moment ngay; nếu `true` thì chỉ stamp. Event này có thể đến ngay sau reload/relaunch (native lưu bền vững), phải xử lý được khi store chưa hydrate xong. |
| L-6 | Gửi `ACTIVITY_COMPLETED` | `routes/index.tsx` `resolve()`, chỗ stamp timer | Khi user tự report "Honoured" (`source: "manual"`), khi timer stamp (`source: "timer"`), khi web tự tính progress ≥ 100% từ `health_daily` (`source: "healthkit"`). `activityId` = `contract.id` cho timer/manual; với goal thì `${contract.id}:primary` / `:secondary` như `health-goals.ts` đang tạo. Không gửi thì native có thể bắn notification "Goal reached" cho contract đã honoured. |
| L-7 | Xử lý `GOAL_REACHED` | listener broadcast | `{ activityId, metric, value, target, reachedAt, notified }` → đánh dấu contract honoured + Completion Moment nếu `notified:false`. `activityId` dạng `<contractId>:primary|secondary` → tách lấy contract. Tối đa 1 lần/activity/ngày, có thể đến sau relaunch. |
| L-8 | Xử lý `NOTIFICATION_OPENED` | listener broadcast | `{ kind: "timer"|"goal", activityId }` → mở tab Contract của contract tương ứng. Đến sau `NATIVE_READY`, có thể là event đầu tiên sau cold start. |
| L-9 | **Setting "Completion sound"** | `Settings.tsx`, `store.ts` | Chưa tồn tại. Toggle mặc định OFF; gửi `SET_SOUND_ENABLED { enabled }` khi đổi **và** một lần sau `NATIVE_READY`. Reply `SOUND_STATE { enabled, gongBundled }`. |
| L-10 | **Gong trong app phải dùng Web Audio** | chỗ chạy Completion Moment | Hiện web chưa phát âm thanh nào khi hoàn thành. Khi thêm: dùng `AudioContext` (tạo/resume trong tap "Start timer", phát lúc hoàn thành), **không** dùng `<audio>` — WebKit sẽ chuyển audio session sang playback và bỏ qua công tắc im lặng. Chỉ phát khi setting ON. |
| L-11 | Sign in with Apple → `linkIdentity` | `Auth.tsx`, `AuthGate.tsx` | Web hiện đăng nhập bằng màn Auth, **không** anonymous-by-default. Cần anonymous session + nút "Save my account" → `SIGN_IN_WITH_APPLE` → `supabase.auth.linkIdentity({ provider: 'apple', token: identityToken, nonce: rawNonce })` (đã kiểm chứng trên docs Supabase 16/09: JS hỗ trợ link bằng ID token native; **phải bật Manual linking (beta)** trong Auth → Providers của project Lovable Cloud; không dùng `signInWithIdToken` vì tạo user mới) → gửi lại `SET_AUTH_SESSION`. Quyết định nghiệp vụ, chờ khách. |
| L-12 | `queryHealthMetrics` `from`/`to` | `native-bridge.ts` | Docstring ghi "Inclusive YYYY-MM-DD" nhưng native yêu cầu ISO 8601 datetime (`from < to`); ngày trần sẽ bị `invalid_range`. Hiện chưa có chỗ nào gọi hàm này (Progress đọc `health_daily`), sửa docstring/format trước khi dùng. |
| L-13 | Ẩn chỉ số giấc ngủ khi rỗng; đọc `health_daily` cho Progress | `health-progress.ts` | Đã đúng hướng (đọc `health_daily`, `null` khi không đo được). |

### 7.1b Tiến độ Lovable — 16/09 (làm qua MCP Lovable, agent Lovable code, mình review diff)

| Commit | Nội dung | Kết quả |
|---|---|---|
| `9672693` "Wired native timer & events" | L-2, L-3, L-4(b), L-5, L-6, L-7, L-8, L-12: 6 outbound + 7 inbound type; `startNativeTimer`/`cancelNativeTimer`/`getNativeTimerState`/`sendActivityCompleted`; parser + `onTimerCompleted`/`onGoalReached`/`onNotificationOpened`; `contractIdFromActivityId` (bỏ `:primary/:secondary`); `timer-store.ts` giao countdown cho native, adopt `endsAt` từ `TIMER_STARTED`, pause = `CANCEL_TIMER` / resume = `START_TIMER(remaining)` (tạm, cô lập trong 2 method), `resolveFinishedTimer` không tự stamp khi có bridge, `applyNativeTimerCompletion` idempotent + gửi `ACTIVITY_COMPLETED timer`, `syncNativeTimerState` sau `NATIVE_READY`/mount; `native-timer.ts` mới: buffer event trước khi store hydrate, `useNativeAppEvents` (Completion Moment khi `notified:false`, mở tab Contract khi `NOTIFICATION_OPENED`); `resolve()` gửi `ACTIVITY_COMPLETED manual`; docstring `from/to` ISO 8601. `tests/timer-bridge.test.ts` 11 test. | typecheck sạch, **102/102** test, build pass. 6.7 credits |
| `513f080` "Added completion sound bridge" | L-9, L-10: `SET_SOUND_ENABLED`/`SOUND_STATE`, `sendSoundEnabled`; `completionSound` trong store (mặc định false), row "Completion sound" trong Settings (cùng style "Challenge Timer"), gửi khi đổi + on mount; `gong.ts` mới **Web Audio only** (comment nêu lý do silent switch), placeholder gong tổng hợp (3 partial 182/196/219 Hz, decay 2.5s), `primeGongAudio()` trong tap Start timer và tap Honoured, `playCompletionGong()` khi `notified:false`. `tests/gong-sound.test.ts` 9 test. | typecheck sạch, **111/111** test, build pass. 5.2 credits |
| `30841c0` "Fixed self-report gong race" | `primeGongAudio()` trả promise của `resume()`; tap "Honoured" tự report await rồi mới phát gong (trước đó chỉ prime, không phát, và `resume()` async nên cùng tick còn `suspended`). | agent báo hoàn tất (response bị ngắt phía mình; đã đọc lại `gong.ts` ở commit này để xác nhận) |

**Review diff phía mình — phát hiện & xử lý**

- Web gửi `ACTIVITY_COMPLETED { activityId: contract.id }` cho timer/manual, còn goal marker native dùng `contract.id:primary/secondary` → contract đã honoured bằng timer vẫn có thể bị "Goal reached" sau đó. **Sửa phía native** (`GoalMonitor.markCelebrated` đánh dấu cả các goal slot có prefix `<id>:`), thêm check vào kịch bản `goals`.
- `GOAL_REACHED` cho **một** slot (`:primary`) → web `honourContract()` cả contract, kể cả khi còn slot `:secondary` chưa đạt. Đúng theo `bridge.md` hiện tại nhưng là câu hỏi sản phẩm → thêm Open Item #10. Nếu khách muốn "cả hai slot", web đổi sang set progress theo slot và chỉ honour khi đủ — không cần đổi native.
- `NATIVE_READY` trả lời `APP_READY` có `requestId` nên bị `onNativeBroadcast` bỏ qua; agent bù bằng `syncNativeTimerState()` lúc mount — đủ cho cold start; broadcast `NATIVE_READY` lúc `didFinish` phủ case reload.

**Còn lại phía Lovable (ghi nhận 16/09):** L-1 publish (quyết định của khách/bạn — có thể `deploy_project` qua MCP), L-11 anonymous + `linkIdentity` (chờ khách). **Đính chính 24/09:** tài liệu Supabase hiện hỗ trợ `linkIdentity` bằng native ID token; ghi chú “chỉ hỗ trợ OAuth redirect” trước đây không còn là kết luận đúng. Vẫn phải kiểm tra SDK web thực tế và Manual linking trước khi code; xem [plan Google Sign-In](google-sign-in-plan.vi.md). Trạng thái deploy/web hiện tại cần kiểm tra lại, không suy từ ghi nhận lịch sử này.

### 7.1c Health UI phía Lovable — 18/09 (kiểm tra + 3 commit qua MCP)

**Câu hỏi của khách: "UI health đã có từ v1?"** — Không. Kiểm bằng cách tải bundle production (`/assets/index-*.js`, `/assets/routes-*.js`) và grep: 0 chuỗi `steps/distance/calories/heart_rate/sleep/Progress/Goal`, 0 message bridge health; chuỗi `Health` duy nhất là icon trang trí trong mockup màn hình chính iOS. Catalogue 44 hoạt động toàn `unit: min`. Dấu vết duy nhất: `type ActivityKind = "steps" | "calories" | …` khai báo từ v1 nhưng 0 entry dùng. Workspace chỉ có một project Honoured (V13), không có bản cũ để đối chiếu. Production hôm nay vẫn v1 (chưa publish `9c13bdf`).

**Ở HEAD `30841c0` trước khi làm:** logic health chạy ngầm (`health-goals.ts`, `health-progress.ts`, `useHealthTrackedProgress` → `setContractProgress`) nhưng **không có UI nào**: Settings không có mục Health, Home chỉ là splash, target UI cap 3 chữ số + đơn vị cố định `min` → 8/9 metric không thể đặt mục tiêu, 39/44 hoạt động không bao giờ tạo goal.

| Commit | Nội dung | Kết quả |
|---|---|---|
| `a683988` "Fixed health day window bug" | **Bug:** `sevenDayWindow()` dùng UTC còn `contractDay()` dùng local + reset hour → ở UTC+10 row hôm nay bị loại 10 giờ đầu ngày. Sửa: `sevenDayWindow(endDay: string)` thuần string, `fetchHealthProgress(…, endDay)`, hook dùng một `day` cho cả query và đo. **Settings → mục "Health":** `health-settings.ts` (`healthConnectionState`: web_preview / loading / unsupported / connect / set_up / unknown), row "Apple Health" với nút Connect → `requestHealthPermission()`; `set_up` là mức tối đa được phép nói (iOS không tiết lộ granted/denied); ẩn khi `available:false`; browser hiện "Available in the iPhone app". | typecheck sạch, **122/122**, build pass. 3.6 credits |
| `8a3287c` (unit picker) | `target-units.ts` mới: `TargetUnit` min/hrs/km/mi/steps/kcal, `UNIT_RULES` (max/step/maxChars/decimals), `sanitizeTargetInput`, `targetNumber`, `formatTarget`, `kindForUnit`. Catalogue đổi `unit` → `units[]`: Running/Walking/Hiking/Rucking/Swimming + **Cycling (mới)** = `[min, km, mi]`; thêm **Steps** `[steps]`, **Active Calories** `[kcal]` (Calories = `active_energy` theo bridge.md cho đến khi chốt Open Item #1). Chip chọn đơn vị dưới ô số cho cả 2 slot; slider/haptic theo rule; recents nhớ unit. `health-goals.ts`: thêm `STEP_UNITS`/`ENERGY_UNITS`, unit thắng label. | **138/138**, build pass |
| `8b8afd1` (sửa review) | Khôi phục `reps` (spec mình bỏ sót — Custom trước là `reps`); hoist `UnitChips` ra module scope (đang khai báo trong render → remount mỗi lần); đổi ô gõ số `type="number"` → `type="text"` + `inputMode` vì `"12."` bị browser coerce thành `""` khi gõ số lẻ. | **139/139**, build pass. 2.5 credits |
| `343d670` (H-9 hiển thị progress đo được) | `health-progress.ts`: `measuredSlots()` per-slot `{slot, metric, value|null, target, unit}`, snapshot store module-level + `useMeasuredContract(contractId)`, publish trong `refresh()` của `useHealthTrackedProgress`, refresh thêm khi `visibilitychange` → visible. `health-display.ts` mới: `formatMeasured()` → "3.2 / 5 km", "1.9 / 3 mi" (theo unit trong target string), "6,240 / 10,000 steps", "320 / 500 kcal", "18 / 30 min", `null` → "No data yet". `Contract.tsx`: card active **thay** dòng target bằng "{đo} · Apple Health" + vạch 1px vàng theo ratio (không thêm dòng, card giữ `h-[265px]`); slot phụ hiện số đo bên phải cùng dòng; `StandingContractRow` bản compact. Browser preview không bridge và chưa có row → giữ text target cũ. | **146/146**, build pass. 4.6 credits |
| `3e3765f` (#10 + empty state) | **Open Item #10 chốt theo luật (b):** `goal-slots.ts` mới (`markReached`/`goalOutcome`, lưu `honoured.goalsReached.v1` theo `(contractId, healthDay)`); `GOAL_REACHED` chỉ `honourContract()` khi **mọi slot Health-mapped** đã đạt trong ngày, slot không map không chặn — khớp `trackedProgress()` và `rollStandingContracts()`; `noteGoalValue()` đẩy số native báo vào card ngay. `formatMeasured` trạng thái rỗng giữ mục tiêu: "5 km · no Health data yet" (trước đó thay mất "5 km"). Đã sửa `bridge.md` đoạn "Web owns contract state". | **155/155**, build pass. 4.4 credits |
| `3d47c08` (fix cold-start) | Review bắt regression: sink đọc contract qua `stateRef` lấy lúc render, trước khi `setStoreUser()` hydrate → `GOAL_REACHED` buffer cold-start bị rớt. Sửa: `store.readStoreState()` + tách `attachNativeAppEvents()`; test `goal-events.test.ts` tái hiện đúng thứ tự event → bind store → attach. | **157/157**, build pass. 2.7 credits |

**Phát hiện khi đọc `store.ts`:** `progress` được `rollStandingContracts()` đọc — standing contract qua reset hour là `honoured` khi `progress ≥ 1`, ngược lại `broken`. `trackedProgress()` lấy **trung bình các slot** nên standing cần đủ cả hai slot; còn `GOAL_REACHED` (native) honour ngay khi **một** slot đạt. Hai đường mâu thuẫn → Open Item #10 cần chốt sớm.

**Còn lại phía Lovable (18/09):**
- L-1 publish — quyết định khách.
- H-9 **đã làm** (`343d670`) trên card contract active + list standing. Chưa làm ở `RestrictionScreen` và `DynamicIsland` — chờ khách xem bản đầu có ưng không.
- H-8 biểu đồ 7 ngày từ `health_daily`: `byMetric()` đã có, chưa có UI — cần khách chốt có muốn màn riêng không.
- H-10 `QUERY_HEALTH_METRICS` cho "hôm nay tới giờ" — chưa gọi.
- H-7 toggle km/mi toàn cục — tạm đủ nhờ chip + recents nhớ unit/hoạt động.
- L-11 anonymous + `linkIdentity`; Open Items #1 (đã tạm chốt Active Calories = `active_energy`), #8 — chờ khách. **#10 đã chốt** luật (b) ở `3e3765f`.
- Câu hỏi sản phẩm mới: hoạt động thể chất nào ngoài 6 hoạt động khoảng cách nên tính `exercise_minutes` (Boxing, Rowing, Soccer…)? Hiện giữ bảo thủ như cũ.

### 7.1d Kết nối Apple Health khi onboard — 21/09 (2 commit qua MCP)

**Trước khi làm:** chỗ duy nhất gọi `REQUEST_HEALTH_PERMISSION` là Settings › Health › Connect. Onboarding (`Intro → AuthGate → NameSetup → Home`) không đụng tới Health; `Contract.tsx` không nhắc gì khi chọn hoạt động map được Health. User mới ký "Running 5 km" mà không vào Settings thì card hiện "5 km · no Health data yet" mãi, native không có gì để đọc. Native không cần thêm message: `REQUEST_HEALTH_PERMISSION` idempotent, xong là `syncNow()` + bật background delivery; `GET_HEALTH_STATUS` cho biết còn metric `notDetermined` hay không.

**Đã chốt:** đặt bước sau đăng nhập + `NameSetup` (native đã có session → sheet đóng là backfill 30 ngày chạy luôn; cờ lưu trong store per-account). Hỏi đủ **9 metric** (khớp `NSHealthShareUsageDescription`). Gate theo **trạng thái HealthKit + cờ per-account**, không theo `needsName` → account cũ cài máy mới và user v1 lên v1.1 đều thấy đúng một lần. Bắt buộc skip được (App Review 5.1.1). Không chữ nào nói "Connected" — tối đa "Set up".

| Commit | Nội dung | Kết quả |
|---|---|---|
| `9dcb52e` (onboarding + nudge) | `health-connection.ts` mới: snapshot status dùng chung (module-level + listeners, dedupe in-flight cho cả `GET_HEALTH_STATUS` và `REQUEST_HEALTH_PERMISSION`), hook `useHealthConnection()`; `HealthSection` trong Settings chuyển sang dùng hook, copy giữ nguyên. `store.ts`: `healthSetupSeen?: boolean`. `health-settings.ts`: `healthSetupOffer(platform, seen, status)` → `skip / loading / offer`. `Onboarding.tsx`: màn `HealthSetup` — heading "Let Apple Health keep the record", 2 dòng giải thích, nút "Connect Apple Health" (→ `requestHealthPermission()` không truyền `metrics`) và "Not now"; kết quả gì cũng `onDone()`. `index.tsx`: hook chạy từ lúc mount để không thêm độ trễ; gate chèn ngay trước Home: `loading` → splash, `offer` → `HealthSetup`, còn lại vào Home. `HealthConnectNudge.tsx` mới ("Apple Health can measure this" + Connect vàng), chỉ hiện khi state `connect`; `health-goals.ts`: `draftHealthGoals()` cho form nháp. `Contract.tsx`: nudge dưới block commitment / trên "Because…" khi draft map ≥1 goal; card active **không thêm dòng** — khi chưa đo được và còn `notDetermined`, suffix "no Health data yet" thành link vàng "Connect Apple Health". Test mới `health-onboarding.test.ts` (gate 5 case, draft goals 3 case, bridge: payload đủ 9 metric, dedupe `GET_HEALTH_STATUS`). | typecheck sạch, **180/180**, build pass. 6.5 credits. Kèm bump `@lovable.dev/vite-tanstack-config` 2.13.1 → 2.23.1 do platform, không yêu cầu — để nguyên. |
| `c9d990e` (dọn review) | Bỏ import thừa `useEffect/useRef` ở `Settings.tsx`; đưa `draftHealthGoals` lên trên doc comment của `goalsForContract` (bị chèn lệch). Agent chạy thêm `eslint --fix` trên các file đã đụng → 112 lỗi prettier có sẵn được reflow, 2 `catch {}` rỗng thêm comment; đã đối chiếu diff: ngoài whitespace không có thay đổi logic. | eslint chỉ còn 3 warning `react-refresh` có sẵn ở `Contract.tsx`; **180/180**. 1.7 credits |

**Thứ tự mới trên iOS:** `Intro → AuthGate → NameSetup (nếu chưa có tên) → HealthSetup (nếu còn metric chưa hỏi và chưa trả lời màn này) → Home`. Browser preview, Android, iPad: không bao giờ thấy màn hay nudge.

**Chưa làm / chờ:**
- Copy tiếng Anh của `HealthSetup` và nudge là bản nháp — khách duyệt.
- Chưa publish (L-1). Chưa chạy trên simulator/máy thật với bản Lovable mới; simulator cần build ad-hoc signing để sheet hiện (xem N-03). Chưa có bằng chứng device cho đường onboarding → sheet → `syncNow()` backfill.
- Native không đổi. Tuỳ chọn về sau: message `OPEN_HEALTH_APP` (mở `x-apple-health://`) cho case đã hỏi rồi mà không có số.

### 7.2 Đã đúng, giữ nguyên

- `sendAuthSession` trên `INITIAL_SESSION / SIGNED_IN / TOKEN_REFRESHED`, `clearAuthSession` trên `SIGNED_OUT`, `expiresAt` Unix giây, nhận `AUTH_SESSION_UPDATED` → `setSession`.
- `sendGoals` full-replacement từ `goalsForContract()` (mapping bảo thủ), `sendDayResetHour` khi đổi giờ.
- `onNativeBroadcast` một listener duy nhất, bỏ qua reply có `requestId`.

---

## 8. Rủi ro & phương án dự phòng

| Rủi ro | Mức | Phương án |
|---|---|---|
| Background delivery không chạy đúng nhịp 1–2h | Cao | Đã thống nhất best-effort; `BGTaskScheduler` làm đường thứ hai; app sync ngay khi mở |
| App Store từ chối vì HealthKit | Trung bình | N-20 làm sớm; chuẩn bị sẵn privacy policy; chừa buffer review trong Tuần 3 |
| Linking Apple làm mất `user_id` ẩn danh | Cao | Test riêng luồng này trên staging trước; đây là lỗi mất dữ liệu, không phải lỗi UI |
| Web app chậm hơn native | Trung bình | Native test bằng trang stub gọi bridge, không chờ web xong mới kiểm thử |
| Live Activity: background execution, HealthKit khóa máy và animation bị giới hạn | Trung bình | Snapshot/stale, local notification + reconcile; không cam kết end đúng giây hoặc blink bắt buộc; xem plan chi tiết N-23–N-27 |
| Cửa sổ M2 đã trôi 2 ngày | Trung bình | Quỹ dự phòng N-19 (10h) là chỗ hấp thụ đầu tiên nếu trễ |

---

## 9. Checklist bàn giao

**M2 (28/09)**
- [ ] Bản build đã nộp App Store, không còn lỗi blocker trên TestFlight
- [ ] 7 metric đọc được, hiện đúng trong `health_daily`
- [ ] Sync nền hoạt động sau khi kill app
- [ ] Offline → online tự đồng bộ lại, không nhân đôi dữ liệu
- [ ] Timer bắn notification đúng khi máy khoá; chạm mở đúng activity
- [ ] Đạt mục tiêu HealthKit ở nền → notification một lần/ngày
- [ ] Tiếng cồng tôn trọng công tắc im lặng, mặc định TẮT
- [ ] Sign in with Apple giữ nguyên dữ liệu của tài khoản ẩn danh
- [ ] `docs/bridge.md` cập nhật đầy đủ bridge v2

**M3 (12/10)**
- [ ] Nhiều Live Activities chạy trên máy thật, một thẻ mỗi contract occurrence; Lock Screen + đủ Dynamic Island presentations
- [ ] Contract mở/Start gần nhất có score cao nhất; đổi focus không đóng thẻ khác hoặc hủy timer
- [ ] Tiến độ Health cập nhật mọi thẻ; partial slot không hoàn thành nhầm contract; trạng thái unknown/stale đúng
- [ ] Final state/celebration phù hợp giới hạn OS; timer suspended/Health khóa máy được QA và mô tả đúng
- [ ] Restore, deep link, user-dismiss và account/day cleanup qua test; chi tiết tại `docs/live-activities-plan.vi.md`
- [ ] Google Sign-In hoạt động trên iOS và Android; sign-in/link đúng UUID, không phá Apple; đạt test matrix trong `docs/google-sign-in-plan.vi.md`
- [ ] Bản cập nhật đã nộp store

---

## 10. Còn cần làm rõ với khách

1. **"Calo" và "Năng lượng vận động" khác nhau thế nào?** Trong HealthKit cả hai đều là `activeEnergyBurned`. Nếu "Calo" nghĩa là tổng (active + basal) thì phải đọc thêm `basalEnergyBurned`. Settings có tuỳ chọn chuyển giữa hai đơn vị này nên cần định nghĩa rõ.
2. **Đã chốt thiết kế 23/09 — Live Activities:** hỗ trợ Lock Screen và Dynamic Island, nhiều thẻ theo contract, contract mở/Start gần nhất được ưu tiên. Giới hạn cập nhật lúc khóa máy/suspend và celebration nằm trong [plan chi tiết](live-activities-plan.vi.md); không dùng yêu cầu “chỉ khi mở khóa” của nháp cũ.
3. **Mục tiêu (goal) lấy từ đâu** khi user chưa ký contract cho ngày hôm đó — kế hoạch Tuần 2 tạm dùng mặc định bảo thủ: không có contract thì không có goal/notification; vẫn cần khách xác nhận nếu muốn dùng goal ngày gần nhất.
4. **Ai publish phần web app và timeline ra sao** — code bridge v2 đã có ở commit `9c13bdf7e6cf5057717d0eaf671519c354790f85`, nhưng bundle production ngày 15/09 chưa có thay đổi này. Đây là đường găng E2E.
5. **Đã giải quyết 14/09:** schema/RLS/RPC đã apply vào Lovable Cloud và test bằng role authenticated; 15/09 kiểm tra lại anon bị chặn đúng `42501`.
6. **Đã giải quyết 15/09:** lấy URL + publishable key công khai từ bundle Lovable và cấu hình vào `.env`/`Config.xcconfig` gitignored.
7. **Auth web hiện tại là màn đăng nhập email, không ẩn danh.** Spec V1.1 muốn ẩn danh mặc định + "Lưu tài khoản". Native chạy được cả hai, nhưng đây là việc bên web cần có người làm. *(thêm 13/09)*

---

## 11. Tiến độ

### 13/09 — Bước 1–4, 6 của thứ tự thực hiện

**Xong (đã commit)**

| Task | Kết quả | Commit |
|---|---|---|
| M1-01 | `docs/bridge.md` viết lại thành v2. So với nháp: thêm nhóm auth session, quãng đường tách 3 metric, đọc thêm `basal_energy`, `GET_TIMER_STATE` + `TIMER_STARTED { endsAt }`, `GOAL_REACHED`, quy tắc `linkIdentity` | `c1b6bed` |
| M1-03 | 9 identifier cho 7 loại dữ liệu, có trong `bridge.md` và `HealthMetric.swift` | `c1b6bed` |
| M1-04 | Khách xác nhận 10/09: local notification, Dynamic Island dời M3, ẩn sleep khi rỗng, chỉ Apple Sign-In | — |
| N-01 | `bridgeVersion = 2`, hàng đợi broadcast (50, sống qua reload), 13 message v2 stub `not_implemented` | `c1b6bed` |
| N-02 (code) | `Honoured.entitlements`, `Info.plist`, `project.yml`, chuỗi `.env → sync-env.sh → xcconfig` cho Supabase. Build simulator pass. | `571de89` |

**Đang làm (chưa commit)**

| Task | Trạng thái |
|---|---|
| N-02 (device) | Chờ bật HealthKit + Sign in with Apple cho App ID `com.jimmy.upwork.honoured` trên Apple portal |
| N-03 | `HealthKitService` quyền + trạng thái, nối `REQUEST_HEALTH_PERMISSION` / `GET_HEALTH_STATUS`. Chờ chạy thử để xác nhận sheet hiện 9 loại |
| N-04 | Anchored read cho 9 metric, anchor commit sau upload, mốc 30 ngày, sleep lọc `asleep*`. Range total bằng `HKStatisticsQuery` (khử trùng iPhone/Watch) |
| N-08 | `QUERY_HEALTH_METRICS` xong (kéo lên từ bước 8). Còn `SET_GOALS`, `SET_DAY_RESET_HOUR` |
| M1-02 | Thiết kế đã sửa (tổng ngày tính trên máy). Cách apply chưa chốt — xem mục 10.5 |

**Phát hiện trong ngày**

- **Web không gửi `APP_READY`** (`OutboundType` trong `native-bridge.ts` chỉ có 5 message billing). Hàng đợi broadcast chỉ flush theo `APP_READY` sẽ kẹt mãi → sửa: bất kỳ message nào từ web cũng đánh dấu ready. `bridge.md` thêm integration notes cho `native-bridge.ts`.
- **iOS không tiết lộ quyền đọc HealthKit** được cấp hay từ chối → `perMetric` là `notDetermined | determined | unknown`, không phải `authorized | denied`. Đã sửa `bridge.md`.
- **iPhone + Watch cùng ghi bước chân** → cộng sample thô đếm đôi. `health_daily.total` phải tính trên máy bằng statistics query. Đã sửa mục 3.2 và 3.3.
- **Supabase của Honoured là Lovable Cloud** (`hsaexzikwubibkwcysnh`), không apply được qua connector. Convention bên web: SQL để ở `docs/db/` như tài liệu mirror, apply thủ công qua dashboard; RPC authenticated-only, trả error object; đã có `trial_state.reset_timezone` per-user.
- **Auth web hiện tại không ẩn danh** (`Auth.tsx`, `AuthGate.tsx`). Web đã có `DynamicIsland.tsx` nhưng là mock visual trong phone frame, không phải Live Activity.
- Simulator: app cài và chạy ổn sau mọi thay đổi, web app render bình thường (ảnh trong tracker).

**Tracker cho khách:** `docs/Honoured-V1.1-Progress-Tracker.xlsx` đã cập nhật cùng nội dung này (bản sống trên Drive). Cột "Actual h" để trống — dev tự điền giờ thật.

### 14/09 — Hoàn thiện phần Tuần 1 không phụ thuộc Apple Developer

**Đã hoàn thành và kiểm tra**

| Task | Kết quả |
|---|---|
| M1-02 / N-05 | Đã apply `health_samples`, `health_daily`, RLS và hai RPC authenticated-only vào đúng database Lovable Cloud; test giao dịch bằng role `authenticated` pass và rollback sạch dữ liệu test. SQL mirror tại `docs/db/healthkit_v1.sql`. |
| N-03 / N-04 | Quyền HealthKit, anchored reads/deletions, tổng ngày theo reset hour và commit anchor sau khi batch được lưu bền vững đã có trong native. |
| N-06 / N-07 | Client upload hai RPC, hàng đợi JSON atomic trong Application Support, retry backoff, tự drain khi có mạng; access token lưu Keychain, refresh trước khi hết hạn và retry một lần khi 401. |
| N-08 | `SET_GOALS`, `SET_DAY_RESET_HOUR`, auth session lifecycle và broadcast `HEALTH_DATA_UPDATED` / `AUTH_SESSION_UPDATED` / `AUTH_SESSION_INVALID` đã nối. |
| Build | `xcodegen generate` và simulator Debug build với `CODE_SIGNING_ALLOWED=NO` đều pass. |

**Đã chủ động skip theo quyết định hiện tại**

- Mọi bước cần Apple Developer account: bật capability trên portal, signing/provisioning và test máy thật.
- Vì vậy điều kiện E2E Tuần 1 (permission sheet + dữ liệu thật + airplane-mode trên iPhone) vẫn chờ khi mở lại nhóm task Apple Developer.

**Web Lovable**

- Database đã xong. Bridge v2, auth hand-off hai chiều, goal mapping bảo thủ và tiến độ từ `health_daily` đã được Lovable triển khai ban đầu ở commit `e9aaf3e5c6f99db257b89a3a9681a7bcf5db571b`; sửa contract `expiresAt` ở commit `9c13bdf7e6cf5057717d0eaf671519c354790f85`.
- Đã audit và sửa các lệch contract ban đầu: `expiresAt` dùng Unix seconds; goal dùng `activityId`/`activityName`; query dùng `from`/`to`; permission reply chỉ dùng `HEALTH_PERMISSION_STATUS` với `perMetric` đúng giới hạn riêng tư của iOS.
- Lovable báo typecheck pass, 91/91 tests pass và production build pass. Chưa publish/deploy.

### 15/09 — Audit lại và khép phần code Tuần 1

**Các khoảng trống đã sửa**

- Khi mạng trở lại, native gọi `syncNow()` để đọc cả dữ liệu Health phát sinh lúc offline rồi mới drain queue; trước đó chỉ drain batch đã có nên case "bật máy bay, đi bộ, bật mạng" có thể không lấy sample mới.
- Retry backoff nay thực sự lặp lại batch sau thời gian chờ, có khóa chống nhiều lần sync/drain chạy song song. Queue rollback trạng thái trong bộ nhớ nếu ghi file atomic thất bại.
- Mỗi batch gắn với `userId`; đổi tài khoản trực tiếp sẽ xoá queue, anchor và goal của user trước. Refresh token cũng bị từ chối nếu Supabase trả về user khác.
- `expiresAt` và target goal phải là số dương hữu hạn; chặn `Infinity`, `NaN` và boolean lọt qua bridge.
- Các đoạn sleep chồng nhau được merge trước khi tính phút để không đếm đôi theo source/stage. Sample đến muộn sẽ làm native tính lại đúng ngày lịch sử liên quan, thay vì chỉ cập nhật hôm nay.
- Đã lấy Supabase URL + publishable key công khai từ bundle Lovable production và đồng bộ vào cấu hình local gitignored. Không ghi key vào tài liệu hoặc Git.

**Xác minh trong ngày**

- `git diff --check` sạch; `Info.plist` và entitlements đều qua `plutil -lint`.
- Supabase production phản hồi `42501`/HTTP 401 cho cả đọc `health_daily` và gọi `upsert_health_daily` bằng role anon, xác nhận bảng/RPC tồn tại và quyền anon bị chặn. Giao dịch authenticated đã được kiểm tra ngày 14/09, không chạy lại vì phiên này không có user token/MCP Supabase.
- Bundle đang deploy tại `honour-your-word.lovable.app` chưa chứa các message bridge v2 (`SET_AUTH_SESSION`, HealthKit, goals). Code Lovable đã pass 91/91 tests ở commit `9c13bdf7e6cf5057717d0eaf671519c354790f85` nhưng vẫn chưa publish/deploy, nên E2E production còn bị chặn ở phía web.
- Simulator build lần này không có kết quả: lệnh build bị dừng theo yêu cầu vì chờ lâu. Kết quả build pass gần nhất vẫn là ngày 14/09; không suy diễn pass cho thay đổi ngày 15/09.
- Test máy thật, signing và Apple Developer capability vẫn deferred. Vì vậy N-02/N-03/N-04/N-06/N-07 giữ trạng thái `In progress` dù phần code đã hoàn tất.

### 15/09 — N-09 đồng bộ nền (HealthKit background delivery)

**Đã code xong**

| Phần | File | Kết quả |
|---|---|---|
| Observer | `HealthBackgroundObserver.swift` (mới) | Một `HKObserverQuery` cho mỗi metric trong 9 loại, đăng ký **đồng bộ** trong `application(_:didFinishLaunchingWithOptions:)` để tồn tại trước khi HealthKit đánh thức app. `enableBackgroundDelivery(.hourly)` idempotent theo từng metric; `disableAllBackgroundDelivery` khi `CLEAR_AUTH_SESSION`. |
| Completion | `HealthObserverCompletion` | Bọc completion handler của HealthKit, khoá `NSLock`, đảm bảo gọi **đúng một lần** dù nhiều đường xử lý. |
| Gộp trigger | `HealthBackgroundDeliveryCoordinator` (actor) | 9 observer bắn cùng lúc chỉ tạo một vòng đọc; callback đến giữa chừng ép thêm một vòng nữa, không bỏ sót. Completion được trả **sau khi batch đã ghi bền vững vào queue**, trước khi thử upload. |
| Tách pipeline | `HealthSyncCoordinator.swift` (viết lại) | `collectAndEnqueue()` (đọc anchored + ghi queue + commit anchor) tách khỏi `drainQueue()` (upload + backoff). Thêm `drainOnce()` chỉ thử một batch, không sleep, dùng cho nền. `syncNow()` foreground vẫn gọi cả hai. Nhiều `collectAndEnqueue()` đồng thời được coalesce bằng waiter list. |
| Trạng thái chờ | `HealthBackgroundPendingState` | Cờ `UserDefaults` đánh dấu có tín hiệu chưa xử lý; xoá khi vòng đọc hoàn tất. `applicationDidBecomeActive` và `applicationProtectedDataDidBecomeAvailable` retry nếu cờ còn — che case app bị treo giữa chừng hoặc máy chưa unlock lần đầu (`errorDatabaseInaccessible` → `.deferred(.protectedDataUnavailable)`). |
| App delegate | `HonouredAppDelegate.swift` (mới), `HonouredApp.swift` | `@UIApplicationDelegateAdaptor`; launch: start observer, bật delivery nếu đã có session, retry pending. |
| Bridge | `NativeBridge.swift` | `SET_AUTH_SESSION` và `REQUEST_HEALTH_PERMISSION` bật background delivery; `CLEAR_AUTH_SESSION` tắt. |
| Tài liệu | `docs/bridge.md` | Thêm mục "Background sync" mô tả hành vi web cần biết. |

**Xác minh**

- `xcodegen generate` chạy lại vì `Honoured.xcodeproj` (sinh 14/09) chưa có hai file mới; sau đó `xcodebuild` simulator Debug `CODE_SIGNING_ALLOWED=NO` → `BUILD SUCCEEDED` (15/09, DerivedData `/private/tmp/honoured-week1-derived`).
- `swiftc -typecheck` sạch; chỉ còn 5 warning `[weak self]` có sẵn trong phần billing v1 của `NativeBridge.swift`, không thuộc thay đổi này.
- `plutil -lint` Info.plist + entitlements OK; entitlement `com.apple.developer.healthkit.background-delivery` đã có từ `571de89`.

**Chưa xác minh / deferred**

- Simulator **không** hỗ trợ HealthKit background delivery, nên toàn bộ hành vi đánh thức chỉ kiểm được trên máy thật với bản build đã ký và provisioning profile có capability Background Delivery. Vẫn chờ nhóm task Apple Developer.
- Vì vậy N-09 giữ `In progress` (code complete, device pending), cùng quy tắc với N-02/N-03/N-04/N-06/N-07.
- Đã xoá hai bản sao rác `ios/Honoured 2.xcodeproj` và `ios/Honoured 3.xcodeproj` (từ 10/09 và 13/09, không phải project canonical).

**Tiếp theo:** N-10 `BGAppRefreshTask` làm đường dự phòng, dùng lại `collectAndEnqueue()` + `drainOnce()`; `Info.plist` đã có `UIBackgroundModes` (`fetch`, `processing`) và `BGTaskSchedulerPermittedIdentifiers` (`$(PRODUCT_BUNDLE_IDENTIFIER).healthsync`).

### 15/09 — N-10 `BGAppRefreshTask` dự phòng

**Đã code xong**

| Phần | File | Kết quả |
|---|---|---|
| Scheduler | `HealthBackgroundRefresh.swift` (mới) | Identifier `<bundle id>.healthsync` khớp `BGTaskSchedulerPermittedIdentifiers`. `register()` chạy đồng bộ trong `didFinishLaunching`; `schedule()` đặt `earliestBeginDate` = +1h (submit lại sẽ thay request cũ, không cần dedupe); `cancel()` huỷ request chờ. |
| Handler | `handle(_:)` | Schedule lần kế **trước** khi làm việc để chuỗi không đứt nếu hết giờ. Chạy `collectAndEnqueue()` → `drainOnce()` (cùng pipeline với observer; không dùng `drainQueue()` vì có sleep backoff tới 300s, vượt ngân sách ~30s của app refresh). `expirationHandler` huỷ `Task` và gọi `setTaskCompleted(false)`. |
| Một lần duy nhất | `BackgroundTaskCompletion` | Khoá `NSLock` bảo đảm `setTaskCompleted` chỉ gọi đúng một lần dù expiration và completion đua nhau. |
| Dừng chuỗi | — | Nếu pass trả `.deferred(.noSession)` hoặc `.deferred(.healthDataUnavailable)` thì huỷ request kế; chỉ `SET_AUTH_SESSION` mới bật lại. `.protectedDataUnavailable` vẫn giữ chuỗi. |
| Lifecycle | `HonouredAppDelegate.swift` | Chuyển `applicationDidBecomeActive` / `applicationProtectedDataDidBecomeAvailable` sang `NotificationCenter` (`didBecomeActiveNotification`, `protectedDataDidBecomeAvailableNotification`) — **sửa lỗi N-09**: app SwiftUI dùng scene lifecycle nên UIKit không gọi các method active/background trên app delegate; retry pending của N-09 trước đây không bao giờ chạy khi app quay lại foreground. Thêm `didEnterBackgroundNotification` → `scheduleIfSessionExists()`. |
| Bridge | `NativeBridge.swift` | `SET_AUTH_SESSION` → `schedule()`; `CLEAR_AUTH_SESSION` → `cancel()`. |
| Info.plist | `UIBackgroundModes` | Bỏ `processing` — không có `BGProcessingTask` nào, khai mà không dùng là lý do bị từ chối theo Guideline 2.5.4. Giữ `fetch` + identifier `.healthsync`. |
| Tài liệu | `docs/bridge.md` | Thêm gạch đầu dòng về app refresh trong mục *Background sync*. |

**Xác minh**

- `swiftc -typecheck` sạch (5 warning `[weak self]` cũ của billing v1). `plutil -lint` Info.plist OK. `xcodegen generate` chạy lại để thêm file mới.
- Simulator Debug build `CODE_SIGNING_ALLOWED=NO` → `BUILD SUCCEEDED`. Info.plist trong bundle build ra: `UIBackgroundModes = [fetch]`, `BGTaskSchedulerPermittedIdentifiers = [com.jimmy.upwork.honoured.healthsync]`, khớp `CFBundleIdentifier + ".healthsync"` mà `HealthBackgroundRefresh.taskIdentifier` tạo lúc chạy.

**Chưa xác minh / deferred**

- App refresh thật chỉ chạy trên máy thật; simulator có thể ép chạy bằng LLDB `e -l objc -- (void)[[BGTaskScheduler sharedScheduler] _simulateLaunchForTaskWithIdentifier:@"<bundle id>.healthsync"]` khi app đang chạy từ Xcode — chưa làm trong phiên này.
- Ngân sách ~30s của app refresh chưa đo với 9 metric + upload thật; nếu lần đọc đầu 30 ngày quá lâu, cân nhắc đọc theo nhóm metric hoặc thêm `BGProcessingTask` cho lần import đầu (khi đó khai lại `processing`).
- N-10 giữ `In progress` (code complete, device pending).

### 15/09 — N-11 hạ tầng notification + N-14 nền (kho event bền vững)

**Đã code xong**

| Phần | File | Kết quả |
|---|---|---|
| Kho event bền vững | `NativeEventStore.swift` (mới) | Actor, file JSON atomic `Application Support/Honoured/native-events.json`, giới hạn 50 (bỏ cũ nhất), payload lưu dạng bytes JSON. `append` ghi đĩa xong mới bắn `NativeEventStore.changed`; `drain()` trả và xoá toàn bộ (oldest-first); `clear()` xoá file. Không bao giờ chứa token. |
| Post 2 đường | `NativeBridgeEvents` | `post()` giữ nguyên (in-memory, dùng cho `HEALTH_DATA_UPDATED`, `AUTH_SESSION_*` vì có token). Thêm `postDurable()` cho `NOTIFICATION_OPENED`, `GOAL_REACHED`, `TIMER_COMPLETED`. |
| Flush đúng một lần | `NativeBridge.swift` | Khi web ready: drain store + hàng đợi in-memory, **merge theo `createdAt`** rồi dispatch (GOAL_REACHED tối qua đi trước NOTIFICATION_OPENED sáng nay). Bridge còn sống mà store có event mới → drain ngay nếu ready. Nếu page reload đúng lúc đang drain → giữ lại vào hàng đợi in-memory, không evaluate vào page đang biến mất. |
| Cô lập tài khoản | `SET_AUTH_SESSION` (đổi user) / `CLEAR_AUTH_SESSION` | Xoá store + huỷ toàn bộ notification đang chờ/đã giao của user trước. |
| Notification | `NotificationCoordinator.swift` (mới) | `install()` set `UNUserNotificationCenter.delegate` đồng bộ trong `didFinishLaunching` (bắt buộc để tap cold-start được giao). `requestPermissionIfNeeded()` chỉ hỏi khi `.notDetermined`, không bao giờ hỏi lại. `schedule(identifier:kind:activityId:title:body:at:sound:)` gắn `userInfo { kind, activityId }`; `cancel(identifiers:)`, `cancelAll()`. |
| Delegate | `willPresent` → `[]` | Notification bắn khi app đang foreground **không hiện**; N-12/N-13 tự bắn event bridge với `notified: false` để web ăn mừng trong app. |
| Deep link | `didReceive` | Chỉ với `UNNotificationDefaultActionIdentifier`; đọc `kind`/`activityId` từ `userInfo` → `postDurable("NOTIFICATION_OPENED")`. Cold start → nằm trong store tới khi web ready. |
| Thời điểm xin quyền | `SET_GOALS` | Sau khi đã reply `GOALS_ACCEPTED` và list không rỗng → `requestPermissionIfNeeded()`. Reply đi trước nên timeout phía web không bị sheet giữ. `START_TIMER` sẽ nối ở N-12. |
| Tài liệu | `docs/bridge.md` | Sửa đoạn *Event queue* (durable cho 3 loại event, cleared khi đổi user) và mục notification (không re-prompt, foreground không hiện). |

**Xác minh**

- `swiftc -typecheck` sạch (5 warning `[weak self]` cũ). `xcodegen generate` thêm 2 file mới. Simulator Debug build → `BUILD SUCCEEDED`.
- `git diff --check` sạch.

**Chưa xác minh / deferred**

- Chưa chạy app trên simulator để kiểm tra luồng tap notification → `NOTIFICATION_OPENED` và thứ tự flush; simulator hỗ trợ local notification nên có thể test sau khi N-12 có `START_TIMER` để tạo notification thật (hiện chưa có gì schedule notification).
- Chưa có Info.plist key nào cần thêm cho local notification. Không cần `remote-notification` background mode.
- N-11 giữ `In progress` (chờ test simulator với timer thật ở N-12); N-14 giữ `In progress` (nền xong, phần navigate phía web và test cold-start máy thật còn lại).

**Lưu ý thiết kế cho N-12/N-13**

- Dùng `NotificationCoordinator.shared.schedule(...)` với identifier ổn định `timer-<activityId>` / `goal-<activityId>-<healthDay>` để reschedule = replace.
- Mọi event hoàn thành (timer/goal) bắn qua `NativeBridgeEvents.postDurable`, không dùng `post`.
- Khi app `.active` lúc timer/goal hoàn thành: huỷ notification và bắn event `notified: false`; delegate `willPresent` chỉ là lưới an toàn.

### 15/09 — N-12 Testament Timer native (+ bridge stub để test simulator)

**Đã code xong**

| Phần | File | Kết quả |
|---|---|---|
| Store một-timer | `TestamentTimer.swift` (mới) | Actor; `ActiveTimer { activityId, activityName, startedAt, endsAt }` lưu `UserDefaults` → sống qua reload và relaunch. `start()` thay timer cũ (trả `replaced`), `cancel()` trả `.cancelled / .nothingRunning / .differentTimerRunning`. |
| Ba đường hoàn thành, một cửa | `complete(_:notified:)` | (1) deadline task `Task.sleep` tới `endsAt` — chỉ hoàn thành nếu `applicationState == .active` (sleep dậy muộn sau suspend không được nhận là in-app); (2) `willPresent` khi notification bắn lúc app foreground → `notified:false`, banner bị ẩn; (3) `reconcile()` lúc launch / `didBecomeActive` / `GET_TIMER_STATE` / tap notification → `notified = isAuthorized()`. Tất cả đi qua `complete()` với guard `current() == timer` trong actor → **`TIMER_COMPLETED` đúng một lần**. |
| Notification | `NotificationCoordinator.schedule` | id `timer-<activityId>`, `userInfo { kind: timer, activityId }`, title "Time's up", body "<activity> — <n> min done." (copy tạm, chờ khách chốt), **sound nil** cho tới N-15 (mặc định setting tắt). Huỷ cả pending lẫn delivered khi cancel/complete → entry trong Notification Center tự dọn. |
| Bridge | `NativeBridge.swift` | `START_TIMER` (validate `activityId`, `activityName`, `durationSeconds` hữu hạn > 0; broadcast `TIMER_CANCELLED` cho timer bị thay **trước** khi reply `TIMER_STARTED`; xin quyền notification **sau** reply nếu `.notDetermined`, rồi re-schedule nếu vừa được cấp). `CANCEL_TIMER` idempotent khi không có timer, `ERROR timer_not_active` nếu đang chạy activity khác. `GET_TIMER_STATE` gọi `reconcile()` trước khi trả. |
| Cô lập tài khoản | đổi user / `CLEAR_AUTH_SESSION` | `TestamentTimer.clear()` + `cancelAll()` notification. |
| Lifecycle | `HonouredAppDelegate` | `reconcile()` lúc launch và `didBecomeActive` (qua NotificationCenter — đúng với scene lifecycle). |
| Tap notification | `NotificationCoordinator.didReceive` | kind `timer` → `reconcile()` trước rồi mới `postDurable(NOTIFICATION_OPENED)`; `createdAt` được đóng dấu lúc gọi nên thứ tự `TIMER_COMPLETED → NOTIFICATION_OPENED` giữ nguyên khi flush. |
| **Bridge stub (DEBUG only)** | `BridgeStub.swift` (mới), `WebViewLoadState`, `NativeBridge` | Trang HTML nhúng thay web app khi launch với `-HonouredBridgeStub`; có nút bấm tay + kịch bản tự chạy qua `-HonouredBridgeScenario <tên>`; log gửi về native bằng `STUB_LOG` → `print` → đọc được qua `xcrun simctl launch --console-pty`. Toàn bộ trong `#if DEBUG`, không vào Release. Đây là "bridge stub" mà ma trận kiểm tra Tuần 2 yêu cầu. |
| Tài liệu | `docs/bridge.md` | Mục Testament Timer: broadcast `TIMER_CANCELLED` khi thay, `CANCEL_TIMER` idempotent, `timer_not_active`, `invalid_timer`, ngữ nghĩa `notified`, relaunch. |

**Xác minh trên simulator (iPhone 17, iOS 26.5) — bằng chứng trong `outputs/week2-timer-20260915/`**

| Kịch bản | Kết quả |
|---|---|
| `timer-foreground` (18 check) | **18/18 PASS**: state ban đầu inactive; `TIMER_STARTED` với `endsAt ≈ now+5s`; state active; `TIMER_COMPLETED { notified:false }` đúng lúc, `completedAt == endsAt`; **không trùng** event; start A → start B → broadcast `TIMER_CANCELLED A` (không `requestId`) rồi `TIMER_STARTED B`; cancel A khi B chạy → `timer_not_active`; cancel B → OK; cancel B lần 2 → idempotent; `activityId` rỗng / duration 0 → `invalid_timer`. |
| Permission timing (N-11) | Sheet "Would Like to Send You Notifications" hiện **sau** `START_TIMER` đầu tiên, không hiện lúc launch (ảnh 01). Lần chạy đầu sheet chặn app ở `.inactive` đúng lúc hết giờ → deadline task bỏ qua đúng thiết kế, `TIMER_COMPLETED` phát khi `GET_TIMER_STATE` reconcile với `notified:false` (chưa có quyền). |
| Background → banner | Start 20s, mở Settings để đưa app xuống nền → banner "Time's up / Background — 20 sec done." hiện đúng giờ (ảnh 02). |
| Nền → foreground cùng process | `didBecomeActive → reconcile()` phát `TIMER_COMPLETED { notified:true }`; xác nhận luôn fix lifecycle NotificationCenter của N-10 chạy thật. |
| Cold start | Start 20s → `simctl terminate` → chờ hết giờ → relaunch: `TIMER_COMPLETED { notified:true }` được flush từ kho bền vững **trước** khi trang hỏi, sau đó `TIMER_STATE { active:false }`. |
| Build | `xcodegen generate`, typecheck sạch, simulator Debug build `BUILD SUCCEEDED`. |

**Chưa xác minh / deferred**

- Tap notification → `NOTIFICATION_OPENED`: chưa bấm được banner từ CLI (osascript không có quyền Accessibility). Có thể test tay: chạy `xcrun simctl launch --console-pty booted com.jimmy.upwork.honoured -HonouredBridgeStub`, bấm "START_TIMER act-1 · 10 s", về Home, chờ banner, chạm banner → log phải có `TIMER_COMPLETED` rồi `NOTIFICATION_OPENED { kind: timer, activityId: act-1 }`.
- Khoá máy thật khi timer chạy, gong (N-15), copy notification chờ khách.
- N-12 giữ `In progress` theo quy tắc chờ máy thật, dù toàn bộ logic đã pass trên simulator.

### 15/09 — N-13 phát hiện đạt mục tiêu ở nền

**Đã code xong**

| Phần | File | Kết quả |
|---|---|---|
| Monitor | `GoalMonitor.swift` (mới) | Actor. `evaluate()`: lấy goals từ `SET_GOALS`, tính `healthDayStart` theo reset-hour, với mỗi goal chưa có marker → `HealthKitService.total(for:from:to:)` (thống kê đã khử trùng, cùng số với `health_daily`, **không** tính lại từ raw sample) → nếu `value >= target`: **ghi marker trước**, rồi nếu app không `.active` và đã có quyền → post notification `goal-<activityId>-<day>`; cuối cùng `postDurable("GOAL_REACHED", { activityId, metric, value, target, reachedAt, notified })`. |
| Một lần/hoạt động/ngày | marker `activityId|yyyy-MM-dd` trong `UserDefaults` | Sống qua relaunch; prune còn hôm nay + hôm qua. Gửi lại `SET_GOALS`, đổi target → không lặp. `activityId` mới cùng metric → bắn riêng. |
| Trigger | `HealthSyncCoordinator.collectAndEnqueue()` | Chạy `evaluate()` khi pass trả `.queued` (có sample mới), **trước** khi completion handler HealthKit được gọi → wake nền không bị suspend trước khi kiểm tra. Cộng thêm sau `SET_GOALS` không rỗng và sau `SET_DAY_RESET_HOUR` đổi giờ. |
| `ACTIVITY_COMPLETED` | bridge | Validate `activityId` + `source ∈ {timer, healthkit, manual}` → `GoalMonitor.markCelebrated()` → reply `ACTIVITY_COMPLETION_ACCEPTED`. Web đã ăn mừng thì native im lặng cả ngày dù metric vượt target sau đó. |
| Đổi reset-hour | bridge | `SET_DAY_RESET_HOUR` với giờ khác → `GoalMonitor.clear()` + `evaluate()` ngay (app đang active → chỉ event in-app, không notification). Cùng giờ → không làm gì. |
| Cô lập tài khoản | đổi user / `CLEAR_AUTH_SESSION` | `GoalMonitor.clear()`. |
| Dùng chung | `HealthSyncSettings.healthDayStart(containing:)`, `dayString(_:)` | Chuyển từ private trong coordinator ra để goal và daily total dùng **cùng một** định nghĩa ngày. |
| Debug | `BridgeStub.fakeHealthTotal(for:)` | `-HonouredFakeHealthTotals steps=9000,active_energy=250` thay kết quả statistics query trong DEBUG để test goal trên simulator không có dữ liệu Health. |
| Tài liệu | `docs/bridge.md` | Mục Goals: thêm "How native decides" (6 quy tắc). |

**Xác minh trên simulator — kịch bản `goals` 11/11 PASS** (`outputs/week2-goals-20260915/goals-scenario.log`)

- 3 goal (walk 8000 steps / burn 400 kcal / swim 500 m) với fake steps=9000, active_energy=250 → chỉ `GOAL_REACHED act-walk { value 9000, target 8000, notified:false }`; burn (250<400) và swim (không có dữ liệu → `nil`) im lặng.
- Gửi lại `SET_GOALS` → không lặp (marker). `ACTIVITY_COMPLETED act-burn manual` → accepted; `source` lạ → `invalid_activity_completion`. Hạ target burn xuống 200 (đã đạt) → vẫn im lặng vì web đã ăn mừng. `act-burn-2` mới cùng metric target 200 → bắn ngay `value 250`.
- Đổi reset-hour → cả `act-walk` và `act-burn-2` được re-announce **đúng một lần** với `notified:false`; gửi lại cùng giờ → không re-announce. Marker sống qua relaunch (lần chạy sau `act-bg-walk` không bắn lại).
- Build simulator Debug → `BUILD SUCCEEDED`.

**Phát hiện: build simulator cần ký ad-hoc để có HealthKit**

- Build `CODE_SIGNING_ALLOWED=NO` không nhúng "simulated entitlements" (`__TEXT,__entitlements`) → `REQUEST_HEALTH_PERMISSION` trả `ERROR "Missing com.apple.developer.healthkit entitlement"`. Build với `CODE_SIGN_IDENTITY=- CODE_SIGNING_REQUIRED=NO` (không cần team) → sheet **Health Access** hiện đúng với các loại đọc (ảnh 03) — bằng chứng simulator đầu tiên cho N-03. Đã ghi vào `AGENTS.md`.

**Chưa xác minh / deferred**

- Nhánh "app không active → post notification, `notified:true`": không ép được trên simulator (sheet Health là remote VC trong process, app vẫn `.active`; không inject được dữ liệu Health để observer/BG refresh trigger từ nền). Nhánh này dùng đúng `NotificationCoordinator.schedule` và check `applicationState` đã pass ở N-12. Cần máy thật + dữ liệu Health thật: đặt goal thấp, đi bộ khi app ở nền → banner "Goal reached" đúng một lần.
- Copy notification "Goal reached / <activity> — 9000 steps of 8000 steps." tạm, chờ khách. Sound `nil` tới N-15.
- Open Item #3 (không có contract hôm nay thì goal nào?) vẫn giữ mặc định bảo thủ: không goal → không thông báo.
- N-13 giữ `In progress` (code + simulator xong, nhánh nền chờ máy thật).

### 15/09 — N-15 tiếng cồng, công tắc im lặng, setting âm thanh

**Đã code xong**

| Phần | File | Kết quả |
|---|---|---|
| Setting | `NotificationSound.swift` (mới) | `sound.enabled` trong `UserDefaults`, mặc định **false**. `current` trả `nil` khi tắt (notification không tiếng, không rung), gong đóng gói khi có, `.default` của hệ thống khi bật mà chưa có asset. |
| Asset gong | `ios/Honoured/gong.caf` (chưa có) | Thả file `.caf/.aiff/.wav` < 30s vào `ios/Honoured/` rồi `xcodegen generate` → XcodeGen tự đưa vào Resources. `SOUND_STATE.gongBundled` cho web biết build có asset hay chưa. **Chờ khách duyệt file.** |
| Bridge | `SET_SOUND_ENABLED { enabled }` → `SOUND_STATE { enabled, gongBundled }` | `enabled` phải là JSON boolean thật (check `CFBooleanGetTypeID`, số/chuỗi → `invalid_sound_state`). Đổi setting khi timer đang chạy → re-register notification cùng identifier với sound mới, `endsAt` giữ nguyên. |
| Timer + goal | `TestamentTimer`, `GoalMonitor` | Notification dùng `NotificationSound.current` thay vì `nil`. |
| Audio session | `HonouredAppDelegate` | `AVAudioSession` category `.ambient` lúc launch — tôn trọng công tắc im lặng, mix với app khác. **Không** dùng `.playback`. |
| Tài liệu | `docs/bridge.md` | Mục sound: boolean thật, gửi sau `NATIVE_READY`, `gongBundled`, fallback default, **Web Audio bắt buộc**. |

**Lưu ý kỹ thuật quan trọng cho web:** WebKit tự chọn category audio session theo nội dung trang phát: Web Audio (`AudioContext`) → giữ ambient; `<audio>`/`<video>` có tiếng → chuyển sang playback và **bỏ qua công tắc im lặng**. Vì vậy gong trong app phải phát qua `AudioContext` (tạo/resume trong tap "Start timer" vì iOS cần user gesture). Notification sound không bị ảnh hưởng — hệ thống tự tôn trọng công tắc.

**Xác minh trên simulator — kịch bản `sound` 5/5 PASS** (`outputs/week2-sound-20260915/`)

- `SET_SOUND_ENABLED true` → `SOUND_STATE { enabled:true, gongBundled:false }`; `enabled: 1` và `"yes"` → `invalid_sound_state`; `false` → OK; timer đang chạy giữ nguyên `endsAt` sau khi toggle.
- Build simulator (ký ad-hoc) `BUILD SUCCEEDED`; typecheck sạch.

**Chưa xác minh / deferred**

- Chưa nghe được tiếng notification (simulator phát qua loa Mac, không kiểm từ CLI). Công tắc im lặng vật lý chỉ test được trên máy thật.
- Chưa có asset gong → khi bật đang dùng tiếng default. N-15 giữ `In progress` chờ asset + máy thật.
- Rà lại code Lovable hôm nay (qua MCP) → checklist đầy đủ ở **mục 7.1** (L-1…L-13). Điểm cần khách chốt: Pause/Resume (L-4), anonymous + `linkIdentity` (L-11), copy notification, asset gong.

### 18/09 — N-16 Sign in with Apple (native)

**Đã code xong**

| Phần | File | Kết quả |
|---|---|---|
| Coordinator | `AppleSignInCoordinator.swift` (mới, `@MainActor`) | `signIn()` async: nonce 32 byte ngẫu nhiên (`SecRandomCopyBytes`, 64 hex) → SHA-256 hex vào `request.nonce`, scope `fullName` + `email`, `ASAuthorizationController` với presentation anchor = key window của scene. `CheckedContinuation` → `Outcome.success(payload) / .cancelled / .failed(message)`. **Một request một lúc**: gọi lần 2 khi sheet đang mở → `failed` ngay, không chồng sheet. |
| Payload | delegate `didCompleteWithAuthorization` | `identityToken` (UTF-8 của `credential.identityToken`), `rawNonce`, `authorizationCode` (nếu có), `user { id, email?, fullName? }` — email/fullName chỉ khi Apple trả (lần đầu), **không gửi chuỗi rỗng**; `fullName` format bằng `PersonNameComponentsFormatter`. Không `print`/log bất kỳ thứ gì. |
| Lỗi | `didCompleteWithError` | `ASAuthorizationError.canceled` → `cancelled`; còn lại → `failed` + `localizedDescription` (an toàn để hiển thị). |
| Credential state | `checkCredentialStateIfNeeded()` lúc launch + `observeRevocation()` (`credentialRevokedNotification`) | Lưu `credential.user` (Apple user id, không phải token) trong `UserDefaults` sau khi thành công; `.revoked`/`.notFound` → xoá và broadcast **`APPLE_CREDENTIAL_REVOKED { userId, reason }`** (in-memory, không bền vững). Web quyết định sign out. |
| Cô lập tài khoản | đổi user / `CLEAR_AUTH_SESSION` | `AppleSignInCoordinator.clear()` quên Apple user id. |
| Bridge | `SIGN_IN_WITH_APPLE` | → `APPLE_SIGN_IN_SUCCESS { … }` hoặc `APPLE_SIGN_IN_FAILED { code: cancelled|failed, message }`, echo `requestId`. |
| Stub | `BridgeStub` | Nút `SIGN_IN_WITH_APPLE` (timeout 120s), kịch bản `apple`; log **redact** `identityToken`/`authorizationCode`/`rawNonce` thành độ dài. |
| Tài liệu | `docs/bridge.md` | Mục Apple: nonce format, absent keys, `failed` khi không có Apple Account / request chồng, `linkIdentity({ provider, token, nonce })` + **bật Manual linking**, timeout dài, broadcast `APPLE_CREDENTIAL_REVOKED`, message index. |

**Xác minh**

- typecheck sạch, `xcodegen generate`, simulator build (ký ad-hoc) `BUILD SUCCEEDED`.
- Simulator (chưa đăng nhập Apple ID): `SIGN_IN_WITH_APPLE` → iOS hiện alert "Sign in to your Apple Account — You need to sign in… in Settings" (ảnh 04 trong `outputs/week2-apple-20260918/`) → chứng minh entitlement `applesignin` được chấp nhận và `ASAuthorizationController` trình bày đúng anchor. Request thứ hai khi sheet đang mở → `APPLE_SIGN_IN_FAILED { code: failed }` ngay (PASS).
- Đã kiểm chứng trên docs Supabase (16/09): JS `linkIdentity` nhận ID token native cho Apple; cần **Manual linking (beta)** bật trong Auth → Providers của project Lovable Cloud `hsaexzikwubibkwcysnh`.

**Chưa xác minh / deferred**

- Luồng thành công thật (token JWT, `rawNonce` 64 ký tự, `user.id`) cần máy thật (hoặc simulator đã đăng nhập Apple ID + App ID có capability trên portal). Kịch bản `apple` đã có check cho payload thành công, chạy khi có thiết bị.
- E2E `linkIdentity` giữ nguyên `userId` ẩn danh: chờ Lovable L-11 (anonymous-by-default, chờ khách) + bật Manual linking.
- `getCredentialState` chỉ có ý nghĩa sau lần đăng nhập thành công đầu tiên; chưa test revoke từ Settings.
- N-16 giữ `In progress`.

**Trạng thái Tuần 2 (18/09):** N-09 → N-16 đã code xong và có bằng chứng simulator cho mọi phần simulator kiểm được; toàn bộ phần còn lại của Tuần 2 là nghiệm thu máy thật (cần Apple Developer capability/signing) và các quyết định phía khách (L-1 publish, L-4 pause/resume, L-11 anonymous, Open Item #10, asset gong, copy notification).

### 18/09 — Cập nhật tracker cho khách (`docs/Honoured-V1.1-Progress-Tracker.xlsx`)

- **Overview:** "Last updated Sep 18". Công thức `% complete` giữ nguyên (đếm `Done`; các task Tuần 1–2 vẫn `In progress` theo quy tắc chờ máy thật nên số % chưa nhảy — cố ý).
- **Weekly Progress:** Week 2 `In progress`, "Delivered so far" gom N-09→N-16 + bằng chứng simulator + trạng thái web (Lovable 16/09); Week 3 ghi sẵn điều kiện bắt đầu (cần build máy thật) và việc làm trước được (N-20).
- **Task Tracker:** N-09→N-16 đều có Evidence (file, kịch bản stub, log/ảnh trong `outputs/`) và Notes tách rõ "code + simulator" với "chờ máy thật". N-14 ghi thêm nửa web đã xong. N-03 có ảnh sheet Health.
- **Screenshots:** **khôi phục ảnh 13/09** (bản Drive gốc 269 KB có ảnh nhúng; bản local do openpyxl lưu 15/09 đã rớt ảnh → lấy lại từ `outputs/week1-progress-20260914/…xlsx`), thêm 4 ảnh Tuần 2: sheet quyền notification (N-11), banner timer khi nền (N-12), sheet Health Access (N-03), sheet Apple (N-16). Ảnh nhúng 193×420 px như bố cục cũ, file 486 KB.
- **Open Items:** #4 và #7 cập nhật (bridge web xong nhưng chưa publish; Apple native xong, chờ anonymous + Manual linking); thêm **#11 Apple Developer access** — blocker của toàn bộ nghiệm thu còn lại; #8 pause/resume, #9 gong + copy, #10 hai slot vẫn mở.
- Bản này là bản cần thay lên Drive (`1OTki6ftXjlOgQcrqmdAz8dM3s-wX75yU`, Manage versions → Upload new version) — **đừng** thay bằng bản 15/09 vì bản đó mất ảnh.

### 18/09 — Rà lại trạng thái `In progress` theo cột "Done when"

Đối chiếu từng task với tiêu chí nghiệm thu ghi trong tracker (không phải "code xong"):

| Task | Tiêu chí | Bằng chứng | Trạng thái |
|---|---|---|---|
| N-11 | Hỏi quyền lần đầu dùng timer, không lúc launch | Ảnh 01 (15/09); launch không hỏi | **Done** |
| N-12 | Countdown sống qua background; cancel gỡ notification | Banner khi nền + `TIMER_COMPLETED` đúng 1 lần + relaunch (15/09); **start → cancel → background → không banner sau `endsAt`** (18/09, ảnh 05, kịch bản `timer-cancel-background`) | **Done** |
| N-03 | Sheet hiện; trạng thái từng loại đúng | Sheet hiện (ảnh 03); trạng thái sau khi người dùng quyết định chưa kiểm — cần một cú bấm trên sheet (sim hoặc máy) | In progress |
| N-15 | Silent switch im lặng; mặc định off | Mặc định off ✅; silent switch vật lý → máy thật | In progress |
| N-02, N-04, N-06, N-07, N-09, N-10, N-13, N-14, N-16 | Máy thật / dữ liệu Health thật / Apple portal / web L-11 | Code + simulator xong; phần còn lại không giả lập được | In progress (thực chất **bị chặn** bởi Open Item #11) |

Tổng sau lần rà đầu: 9 Done / 11 In progress / 17 Not started.

**Quyết định 18/09 (dev):** trong tracker, `Done` = phần việc của task đã làm xong và kiểm được ở mức môi trường cho phép; nghiệm thu máy thật là việc của Tuần 3 (N-17/N-18) và bị chặn bởi Open Item #11, không giữ task Tuần 1–2 ở `In progress` vì lý do đó nữa. Áp dụng: N-02, N-03, N-04, N-06, N-07, N-09, N-10, N-13, N-14, N-15, N-16 → **Done**, Notes của từng task ghi rõ phần đã kiểm và phần dời sang Tuần 3 / chờ khách (N-15 asset gong, N-16 anonymous + Manual linking). Week 1 và Week 2 → `Done`.

**Tổng hiện tại: 20 Done / 0 In progress / 13 Not started** (Tuần 3: N-17→N-22, M3: N-23→N-29). Overview: M1 100%, M2 tự tính theo formula.

### 18/09 — Info.plist + privacy manifest (chuẩn bị máy thật / App Store, một phần N-20)

**Rà `Info.plist`** — đã đủ cho mọi feature: `NSHealthShareUsageDescription`, `NSMicrophoneUsageDescription`, `UIBackgroundModes = [fetch]`, `BGTaskSchedulerPermittedIdentifiers`, custom key Supabase/RevenueCat/web URL, iPhone-only, portrait. Sign in with Apple và local notification không cần key plist. Bổ sung:

| Key | Giá trị | Vì sao |
|---|---|---|
| `NSHealthUpdateUsageDescription` | "Honoured only reads your Health data and never writes to it." | App không ghi Health và không bao giờ hiện chuỗi này, nhưng binary tham chiếu `requestAuthorization(toShare:read:)` → upload lên App Store Connect bị **ITMS-90683 "Missing purpose string"** nếu thiếu. |
| `ITSAppUsesNonExemptEncryption` | `false` | Chỉ HTTPS, không mã hoá riêng → miễn export compliance; mỗi build TestFlight không phải trả lời bảng hỏi encryption. |

**Thêm `ios/Honoured/PrivacyInfo.xcprivacy`** (XcodeGen tự đưa vào Resources, đã thấy trong bundle build):
- `NSPrivacyAccessedAPITypes`: `UserDefaults` lý do `CA92.1` — API duy nhất thuộc nhóm "required reason" mà code native dùng (rà `attributesOfItem`/`modificationDate`/`systemUptime`/disk space: không có). Thiếu mục này upload bị ITMS-91053. RevenueCat SDK có manifest riêng.
- `NSPrivacyCollectedDataTypes` (bản nháp cho nutrition label, **chốt lại ở N-20**): Fitness (bước, quãng đường, năng lượng, phút vận động), Health (nhịp tim, giấc ngủ), User ID (Supabase + RevenueCat), Email + Name (từ Sign in with Apple, lần đầu), Purchase History (RevenueCat), Audio (Voice of Change qua quyền micro của shell). Tất cả *linked to user*, *không tracking*, mục đích *App Functionality*. `NSPrivacyTracking = false`, không tracking domain.

**Cố ý không thêm**
- `UIRequiredDeviceCapabilities = healthkit`: app vẫn chạy khi không có HealthKit (bridge trả `available:false`), target đã iPhone-only.
- `NSHealthRequiredReadAuthorizationTypeIdentifiers`: sync là best-effort, không ép người dùng cấp.
- `UIUserInterfaceStyle = Dark`: sheet/alert hệ thống hiện đang sáng trên nền app đen — là quyết định thiết kế, để khách chốt.

**Chưa đổi:** `CFBundleShortVersionString` vẫn `1.0.0` / build `1` — cần chốt số version V1.1 với khách (web Settings cũng đang hiện 1.0.0) trước N-21.

Build simulator `BUILD SUCCEEDED`; `plutil -lint` cả hai file OK; kiểm tra bundle build ra có `PrivacyInfo.xcprivacy` và hai key mới.

### 24/09 — M3: nhiều Live Activities (phần native)

- Đã triển khai phần native theo [plan nhiều Live Activities](live-activities-plan.vi.md): tracking store, bridge/capability, Widget Extension và UI, tích hợp timer/Health, deep link, restore, dọn theo account/ngày. Chi tiết, các điểm điều chỉnh so với plan và bằng chứng ở mục 14 của plan đó; hợp đồng message ở `docs/bridge.md`, mục *v2 — Live Activities*.
- LA-01 đối chiếu trên bundle web production đang deploy (không phải source): contract honoured khi mọi slot Health đạt trong ngày, khi timer xong tự nhiên, hoặc tự báo; web hiện chỉ cho một contract active và chỉ gửi `SET_GOALS` của contract đầu tiên.
- Kiểm chứng: build simulator app + widget pass; unit test mới `HonouredTests` 81/81 (kèm 4 lỗi Medium từ review độc lập đã sửa); các kịch bản Live Activity qua BridgeStub trên simulator (ActivityKit thật) pass; hồi quy `timer-foreground`, `timer-cancel-background`, `goals`, `sound` pass. Android build pass.
- Còn lại: tích hợp web LA-06 (chưa sửa, chưa publish), QA máy thật, App ID/profile cho extension và TestFlight.
- Version app và extension nay lấy từ `MARKETING_VERSION` / `CURRENT_PROJECT_VERSION` trong `ios/project.yml` (vẫn 1.0.0 / 1).

### 24/09 — Sắp xếp lại thư mục code iOS/Android

- iOS: 25 file Swift nằm phẳng trong `ios/Honoured/` được chia theo tính năng: `App/`, `WebView/`, `Bridge/`, `Auth/`, `Billing/`, `Health/`, `Timer/`, `Notifications/`, `Debug/` (BridgeStub); `LiveActivities/` giữ nguyên. `Info.plist`, entitlements, privacy manifest và `Assets.xcassets` vẫn ở gốc target. Chỉ dời file, không sửa code; `ios/project.yml` chỉ đổi đường dẫn `HealthMetric.swift` của target test.
- Android: `NativeBridge.kt` sang package `com.honoured.app.bridge`, `SubscriptionService.kt` sang `com.honoured.app.billing`; chỉ đổi `package`/`import`. `MainActivity` và `AppConfig` giữ ở `com.honoured.app` nên manifest không đổi.
- Đường dẫn trong các ghi chú trước mục này theo bố cục phẳng cũ (ví dụ `ios/Honoured/NativeBridge.swift` nay là `ios/Honoured/Bridge/NativeBridge.swift`). Sơ đồ hiện tại ở README, mục *Repository layout*.
- Kiểm chứng: sinh lại project, mỗi target vẫn gồm đúng các file như trước (65 mục, so theo tên file); unit test 81/81; build app + widget pass; kịch bản `live-activities-multiple` trên simulator 20/20; Android `assembleDebug` pass.
